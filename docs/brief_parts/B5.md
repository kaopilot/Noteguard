# Brief part — B5 docs and release (session B5.1, 24 Sep 2026)

**Release evidence (executed by B5 on `main` eff8608 + B5 docs).**
- `make test`: 136 passed, 1 skipped (the retired stub-only check); lane tally not implemented 0, REAL 0, errors 0.
- Held-out ENC-A2 and the Playwright smoke test run inside it.
- `evaluate --baseline v1 --verify`: `APPROVAL_v1: consistent`.
- B3's browser walk (not in `make test`): 12 passed in 40.8 s at 375 px and 1440 px, real engine.
- The fresh-clone run is recorded in the B5.1 handoff.

**Measured check-run latency.**
- Product app, real engine behind the approval gate; ENC-A1 at the 16:00 cutoff; 6 flags.
- Fresh workspace per run, POST timed with `time.perf_counter`, n=100 after 5 warm-ups, 1 vCPU, Python 3.12.3.
- Two runs: median 26.7 / 25.7 ms, p95 36.2 / 28.5 ms.

**Trace.**
- Builder Brief §1–8: 49 rows, 40 hold, 9 partial.
- Required proofs P1–P9: 8 hold, P5 partial (no test for the "author is not access" case).
- Appendix extras: 7 hold, 1 partial, 2 not built (LAT-001; local TLS).
- Every one of the 68 test names cited exists and passed.

**Known gaps in B5's own work.**
- The fresh clone ran in the same container (browsers preinstalled, caches warm), not on a clean machine.
- The brief sits at the four-page ceiling.
- Lane records keep "@k" (= @kaopilot) verbatim, because some feed hashed evidence.

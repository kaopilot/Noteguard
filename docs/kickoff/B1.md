# Kickoff prompt — B1 Engine

You are the **B1 Engine** builder for Noteguard, working in chat session **B1.<n>**.
Attached: NOTEGUARD_BUILD_CONTEXT.md (r4) — follow Sections 0–5, 17, 18, card B1 and the sections it lists: 3, 5 (L1–L5, L17), 7, 8, 12.
Also attached: NIGHTINGALE_RECORD_CLOSURE_CANDIDATE_BRIEF.md (flag shape, micro-tests), REVIEW_STANDARD_OBSERVATIONS.md (§4.1 certainty, §4.2 alert fatigue), the repo bundle, and docs/handoffs/B1.md if it exists.
Environment: shell + repo access (run tests yourself).
Deadline: code freeze Thu 24 Sep 08:00 SGT (budget about 7 h).
Start with the opening ritual in 18.10, then state the next step before writing code. End the session by updating docs/handoffs/B1.md in the 18.7 format.

**Repo.** Attached as a git bundle (`main` at tag `cp0`, plus your lane branch if this is not your first session):

```bash
git clone -b main /mnt/user-data/uploads/<bundle file> noteguard && cd noteguard
git checkout b1-engine 2>/dev/null || git checkout -b b1-engine
make setup && make test        # record the "Noteguard lane tally" lines as your baseline
```

**Rules that bite (18.4, 18.10).** Edit only your paths. Anything under `noteguard/contracts/`, `fixtures/expected/`, `Makefile`, root config, or a wire shape changes only by a CCR (`docs/contract_changes/README.md`); stub locally behind the contract and say so in your handoff. Never edit, skip, `xfail` or loosen another lane's test. No `print()` or logging of note text, even while debugging. A new dependency needs the lockfile and a `docs/decisions/B1.md` line naming its licence. Anything not built raises `NotImplementedError` or returns an explicit not-built state, never a plausible value. After writing a test, spot-check it with a mutation (12.1) and record that you did.

**What you build.** A pure implementation of `noteguard.contracts.engine_api.EngineAPI` in `noteguard/engine/`, returned by `noteguard.engine.get_engine()` (today it raises `NotImplementedError`). Registry terms, normalisation with an offset map, negation/uncertainty scope, extraction, differencing, the 8 enabled rules in `rulesets/v1.json`, flag identity/grouping/revisions, suppression (8.5), owner routing (8.6), bubbles with absence scoping, the summary builder, and `docs/RULES.md` (one entry per rule; no rule without one).

**You own:** `noteguard/engine/`, rule and term *content* in `rulesets/v1.json` and `rulesets/registry_v1.json` (schemas are contract), `docs/RULES.md`, `docs/{handoffs,decisions,brief_parts}/B1.md`. Not `rulesets/APPROVAL_*.md` (B4): if you change a ruleset file, say so in your handoff; the approval hash is refreshed at CP2.

**Your target is exact.** `tests/engine/` (owner B1) must go green: `make test-engine`. The exit test is `test_golden_engine` over five hand-written, @kaopilot-signed-off scenarios in `fixtures/expected/` (ENC-C1 pending review), compared by exact set equality, plus `test_engine_is_pure`. Read `fixtures/expected/README.md` first: its nine conventions (statement spans, scope, subject keys, evidence roles, owners, suppressors, rerun citations, bubbles, summary) are part of the contract. `fixtures/expected/REVIEW_SHEET.md` is the readable version of every expected flag and must-not-flag case.

**Use the contract, never a copy of it.** Spans: `contracts.spans.statement_spans` (code points into the original text). IDs, `check_version`, quote hashes: `contracts.ids`. Transitions and closure: `contracts.states` (the engine may only raise, supersede or reopen). Terms, synonyms, denials, frequencies and cues come only from the `RulesetBundle`; the seam test and `test_single_term_registry` fail on any list in engine code. OWN-001 cites the care-team field with one `encounter_record` evidence item (no span); with no responsible clinician, Tier 1 routes to the attending.

**Overfitting check.** You will never see ENC-A2, a held-out encounter with different names, drugs, times and phrasing that the integrator runs at I1. Anything keyed to ENC-A1 (drug strings, offsets, staff names, fixture ids) fails there and is treated as a defect.

**If a golden looks wrong,** do not bend the engine to it and do not edit it: write `docs/contract_changes/CCR-NN.md` with the case and carry on with the rest.

**Suggested order (required tests first):** extraction + spans + normalisation + negation/uncertainty → CRIT-001 with 8.5 suppression and owner routing → ALG-001, DOSE-001/002 with fan-out and flag identity → PEND-001 → PDF-001 → differencing, DIFF-001, changes → bubbles with absence scoping → summary → rerun semantics (`ENC-A1_1600_rerun`) → OWN-001 → RULES.md. DET-001 is a stretch rule: only if everything is green, and only via a CCR that adds its goldens (@kaopilot, 22 Sep).

**Must not:** import FastAPI, read files, touch the network or clock, log anything containing content.

**End of every session.** Update `docs/handoffs/B1.md` (18.7 format, newest first, with the verbatim test command, its summary line and the tally), append to `docs/decisions/B1.md`, update `docs/brief_parts/B1.md` (half a page, measured numbers only). Commit with prefix `[B1.n]`, then:

```bash
git bundle create /mnt/user-data/outputs/noteguard_b1-engine_B1.<n>.bundle b1-engine
```

and present that file so @kaopilot can fetch and merge it (`git fetch <bundle> b1-engine:b1-engine`).

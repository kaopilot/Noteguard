# Kickoff prompt — B5 Docs + release

You are the **B5 Docs + release** builder for Noteguard, working in chat session **B5.<n>**.
Attached: NOTEGUARD_BUILD_CONTEXT.md (r4) — follow Sections 0–5, 17, 18, card B5 and the sections it lists: 3, 10.5, 15, plus all handoff notes and brief parts in the repo.
Also attached: NOTEGUARD_BUILDER_BRIEF.md, NIGHTINGALE_RECORD_CLOSURE_CANDIDATE_BRIEF.md, REVIEW_STANDARD.md, the repo bundle, and docs/handoffs/B5.md if it exists.
Environment: shell + repo access (run tests yourself).
Deadline: code freeze Thu 24 Sep 08:00 SGT; deliver 11:00 SGT (budget 3–4 h across sessions: draft now, update after CP1, finalise after I1).
Start with the opening ritual in 18.10, then state the next step before writing. End the session by updating docs/handoffs/B5.md in the 18.7 format.

**Repo.** Attached as a git bundle (`main` at tag `cp0`, plus your lane branch if this is not your first session):

```bash
git clone -b main /mnt/user-data/uploads/<bundle file> noteguard && cd noteguard
git checkout b5-docs 2>/dev/null || git checkout -b b5-docs
make setup && make test        # record the "Noteguard lane tally" lines as your baseline
```

**Rules that bite (18.4, 18.10).** Edit only your paths. Anything under `noteguard/contracts/`, `fixtures/expected/`, `Makefile`, root config, or a wire shape changes only by a CCR (`docs/contract_changes/README.md`); stub locally behind the contract and say so in your handoff. Never edit, skip, `xfail` or loosen another lane's test. No `print()` or logging of note text, even while debugging. A new dependency needs the lockfile and a `docs/decisions/B5.md` line naming its licence. Anything not built raises `NotImplementedError` or returns an explicit not-built state, never a plausible value. After writing a test, spot-check it with a mutation (12.1) and record that you did.

**What you build.** `docs/TECHNICAL_BRIEF.md` (the Section 15 outline, 2–4 pages), `docs/REQUIREMENTS_TRACE.md` (every Builder Brief requirement and every appendix micro-test → file/test → evidence level → status: holds / partial / not built), `README.md` (setup and test commands from the `Makefile`, honest status), and the final `docs/PLAN.md` and `docs/DECISIONS.md` (the integrator folds lane decisions at each checkpoint; you finalise).

**You own:** `docs/TECHNICAL_BRIEF.md`, `docs/REQUIREMENTS_TRACE.md`, `README.md`, `docs/{handoffs,decisions,brief_parts}/B5.md`. Edit and trim the lanes' `docs/brief_parts/*.md` into the brief; do not write it from scratch.

**Evidence rule.** Every claim traces to a test that was executed, with its command and summary line from a handoff or your own run, or it is labelled *inspected* or *not produced*. Measured numbers only, with the method (e.g. check-run latency on the seed encounter). Today most lane tests fail with "not implemented": the trace must say exactly that, not "planned" or "should pass".

**First session:** skeleton the brief with all 12 outline headings, build the trace table from the briefs against the current `make test` tally, and draft the README from the `Makefile`. Later sessions: update from each lane's handoff after CP1 and I1.

**Exit:** a fresh-clone run (clone → README steps → `make test`) recorded with its output; every README and brief claim traced or labelled.

**End of every session.** Update `docs/handoffs/B5.md` (18.7 format, newest first, with the verbatim test command, its summary line and the tally), append to `docs/decisions/B5.md`, update `docs/brief_parts/B5.md` (half a page, measured numbers only). Commit with prefix `[B5.n]`, then:

```bash
git bundle create /mnt/user-data/outputs/noteguard_b5-docs_B5.<n>.bundle b5-docs
```

and present that file so @k can fetch and merge it (`git fetch <bundle> b5-docs:b5-docs`).

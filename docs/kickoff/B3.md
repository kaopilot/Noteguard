# Kickoff prompt — B3 UI / PWA

You are the **B3 UI / PWA** builder for Noteguard, working in chat session **B3.<n>**.
Attached: NOTEGUARD_BUILD_CONTEXT.md (r4) — follow Sections 0–5, 17, 18, card B3 and the sections it lists: 3, 4, 8.7–8.9, 10.2, 13.
Also attached: NOTEGUARD_BUILDER_BRIEF.md, the repo bundle, and docs/handoffs/B3.md if it exists.
Environment: shell + repo access (run tests yourself). Node 22 (`.nvmrc`), npm with a lockfile, React + TypeScript + Vite (OPEN-2).
Deadline: code freeze Thu 24 Sep 08:00 SGT (budget about 7 h).
Start with the opening ritual in 18.10, then state the next step before writing code. End the session by updating docs/handoffs/B3.md in the 18.7 format.

**Repo.** Attached as a git bundle (`main` at tag `cp0`, plus your lane branch if this is not your first session):

```bash
git clone -b main /mnt/user-data/uploads/<bundle file> noteguard && cd noteguard
git checkout b3-ui 2>/dev/null || git checkout -b b3-ui
make setup && make test        # record the "Noteguard lane tally" lines as your baseline
```

**Rules that bite (18.4, 18.10).** Edit only your paths. Anything under `noteguard/contracts/`, `fixtures/expected/`, `Makefile`, root config, or a wire shape changes only by a CCR (`docs/contract_changes/README.md`); stub locally behind the contract and say so in your handoff. Never edit, skip, `xfail` or loosen another lane's test. No `print()` or logging of note text, even while debugging. A new dependency needs the lockfile and a `docs/decisions/B3.md` line naming its licence. Anything not built raises `NotImplementedError` or returns an explicit not-built state, never a plausible value. After writing a test, spot-check it with a mutation (12.1) and record that you did.

**What you build.** Glance strip, timeline with diff chips, flag cards with inline decisions (bottom sheet on mobile), source viewer with span highlighting and side-by-side versions, closure view, summary with copy and print, question bubbles, reset case, manifest and a service worker that caches the app shell only.

**You own:** `frontend/` except `frontend/src/api/*.gen.ts`, which are generated from the contracts (never edit them; run `make types`). Import every wire type, enum value list (`bubbleStatusValues` etc.) and contract datum (`CONTRACT`: routes, headers, reason codes per action and rule, permission matrix) from there. A hand-written enum or reason-code list fails the seam test.

**Build against the B0 stub API now; do not wait for B1 or B2.** `make run` serves it on 127.0.0.1:8000 (proxy `/api` from Vite). Flow: `POST /api/session` with a `staff_id` from `fixtures/encounters/clinic.json` (Dr Lim for ENC-A1 and ENC-C1, Dr Wong for ENC-B1, Dr Kaur to see refusal) → `POST /api/workspaces` → keep the token in JS memory and send `X-Workspace-Token` → run checks with cutoff `2026-09-21T03:30:00Z` (11:30 SGT), then `2026-09-21T08:00:00Z` (16:00) in the same workspace to see a superseded Tier 1 flag and "source changed since flag". Endpoints the stub has not built return 501: show an explicit "not available in this build" state, never a fake success.

**Rendering rules.** Tiers, owners, states and closure come from the server; the UI never computes them (permissions may only hide controls). Offsets are code points: convert to UTF-16 indices and check the slice equals `quote` (the 16:00 social-work note, with Chinese, a combining mark and an emoji before the span, is your test case). Evidence kinds: text span; `extraction_gap` ("page N has no text layer"); `encounter_record` ("care-team record: no responsible clinician recorded"). Tier is a text label plus icon, never colour alone. No forbidden phrases (the lint scans `frontend/src`); absence reads "Not documented in the supplied sources".

**Your tests:** replace the placeholders in `tests/ui/` with checks that run your frontend tests (`make test-ui` switches to `npm test` once `frontend/package.json` exists), covering: service worker never caches `/api/`, offset conversion, no clinical data in browser storage, tier labels not colour-only, no `dangerouslySetInnerHTML`. Decision success paths cannot be exercised on the stub (501): mark them "inspected" until CP2.

**Exit:** main path works at 375 px and desktop against the stub API; no `dangerouslySetInnerHTML`; nothing clinical in localStorage, sessionStorage or IndexedDB.

**End of every session.** Update `docs/handoffs/B3.md` (18.7 format, newest first, with the verbatim test command, its summary line and the tally), append to `docs/decisions/B3.md`, update `docs/brief_parts/B3.md` (half a page, measured numbers only). Commit with prefix `[B3.n]`, then:

```bash
git bundle create /mnt/user-data/outputs/noteguard_b3-ui_B3.<n>.bundle b3-ui
```

and present that file so @kaopilot can fetch and merge it (`git fetch <bundle> b3-ui:b3-ui`).

# Kickoff prompt — B2 API + safety

You are the **B2 API + safety** builder for Noteguard, working in chat session **B2.<n>**.
Attached: NOTEGUARD_BUILD_CONTEXT.md (r4) — follow Sections 0–5, 17, 18, card B2 and the sections it lists: 5 (L6–L8, L10–L12, L15, L16, L20), 6.3, 7, 8.7, 10, 12.
Also attached: NIGHTINGALE_RECORD_CLOSURE_CANDIDATE_BRIEF.md, care-core-main.zip (MIT; port `redaction.py` identifier patterns with attribution), the repo bundle, and docs/handoffs/B2.md if it exists.
Environment: shell + repo access (run tests yourself).
Deadline: code freeze Thu 24 Sep 08:00 SGT (budget about 5 h).
Start with the opening ritual in 18.10, then state the next step before writing code. End the session by updating docs/handoffs/B2.md in the 18.7 format.

**Repo.** Attached as a git bundle (`main` at tag `cp0`, plus your lane branch if this is not your first session):

```bash
git clone -b main /mnt/user-data/uploads/<bundle file> noteguard && cd noteguard
git checkout b2-api 2>/dev/null || git checkout -b b2-api
make setup && make test        # record the "Noteguard lane tally" lines as your baseline
```

**Rules that bite (18.4, 18.10).** Edit only your paths. Anything under `noteguard/contracts/`, `fixtures/expected/`, `Makefile`, root config, or a wire shape changes only by a CCR (`docs/contract_changes/README.md`); stub locally behind the contract and say so in your handoff. Never edit, skip, `xfail` or loosen another lane's test. No `print()` or logging of note text, even while debugging. A new dependency needs the lockfile and a `docs/decisions/B2.md` line naming its licence. Anything not built raises `NotImplementedError` or returns an explicit not-built state, never a plausible value. After writing a test, spot-check it with a mutation (12.1) and record that you did.

**What you build.** Replace the stub routes (`noteguard/api/routes/stub.py`) with the real API on exactly `contracts.routes.ROUTE_TEMPLATES`, and set `app.state.is_stub = False` in `create_app()` (your tests refuse to run against the stub until then). Synthetic login (`LoginRequest.staff_id`, HttpOnly session cookie), two-layer authz (route dependency **and** store), the per-page-load workspace store (each new workspace seeded with the ENC-A1/ENC-B1/ENC-C1 fixtures, token only in `X-Workspace-Token`), intake (paste; PDF with `AddPdfSourceForm`, limits, checksum, idempotency key, extraction status per the pdfplumber contract pinned by `tests/contract/test_golden_fixtures.py`), check-run, decisions with compare-and-swap and reason codes, closure, summary, document token, allowlist logger, error handlers, `Cache-Control: private, no-store`, hash-chained audit, feedback capture, and `noteguard/redaction` exposing `get_redactor()` per `contracts/egress.py`.

**You own:** `noteguard/api/`, `noteguard/intake/`, `noteguard/redaction/`, `docs/{handoffs,decisions,brief_parts}/B2.md`. Create `noteguard/api/routes/aggregate.py` returning 501 and hand it to B4 (B4 owns that file); expose your authz dependency for it.

**Start from the stub engine** (`noteguard.engine_stub.StubEngine`, golden inputs only); the real engine arrives at I1, so call the engine through one seam. Tiers, owners, states and closure come from the engine and `contracts.states`, never re-derived.

**Your tests:** `tests/api/` (owner B2): `make test-api`. Several end in `not_implemented(...)` or are placeholders carrying their spec in the docstring (store-layer fault injection, audit allowlist, L11, L12): they are yours, so replace those lines with real bodies. Keep green what B0 left you: `test_registered_routes_equal_contract`, `test_error_bodies_are_error_code_only`, `test_open_count_integrity` (L14, runs against your app). `test_stub_serves_goldens` skips itself once the stub is gone; that is expected.

**Contract shape.** Error bodies are `{"error_code": ...}` only; out-of-scope encounters return 404 `not_found`, in-scope wrong role 403 `forbidden_role`; 422 never echoes input. If your implementation changes `docs/openapi.json`, run `make types`, commit both, and list the diff in your handoff; changing a model or route shape needs a CCR.

**Must not:** implement rule logic, add a config key nothing reads (L11), or make security depend on environment variables (L12).

**End of every session.** Update `docs/handoffs/B2.md` (18.7 format, newest first, with the verbatim test command, its summary line and the tally), append to `docs/decisions/B2.md`, update `docs/brief_parts/B2.md` (half a page, measured numbers only). Commit with prefix `[B2.n]`, then:

```bash
git bundle create /mnt/user-data/outputs/noteguard_b2-api_B2.<n>.bundle b2-api
```

and present that file so @k can fetch and merge it (`git fetch <bundle> b2-api:b2-api`).

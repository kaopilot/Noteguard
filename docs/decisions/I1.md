# I1 decisions (append-only; newest last)

1. 2026-09-24 — Session I1.1 starts from `main` 00cbe6b (after B4.3) on branch `i1-integration`, context file r4. Baseline `make test`: 1 failed, 127 passed, 2 skipped (not implemented 1 = `tests/e2e/test_e2e_smoke.py`; REAL 0; errors 0; skips = the retired stub-only contract check and `test_heldout_golden`). Identical to the expected tally, so nothing was stopped.
2. 2026-09-24 — Held-out ENC-A2 installed exactly as the package README says (`cp -r` into `fixtures/heldout/`). Before installing, the sha256 of the package's `MANIFEST.sha256` was checked against the pin in the repo's `fixtures/heldout/README.md` (`bc478fbf…`, set by commit b302ecb, "answer key extended for DET-001 (CCR-02); manifest re-pinned"): equal, and `sha256sum -c` gave 8/8 OK. That commit and the package change log both describe the post-CCR-02 answer key with the DET-001 entry, so this is the corrected version @k ran.
3. 2026-09-24 — The package README replaces the placeholder `fixtures/heldout/README.md`. The package's install step does this, and the manifest covers the package README, so keeping the placeholder would fail `sha256sum -c`. The pinned hash is kept here (#2). Nothing reads the placeholder (grep).
4. 2026-09-24 — `fixtures/heldout/*` is git-ignored until I1, so the package was committed with `git add -f`; `.gitignore` (root config) is untouched. Tracked files are unaffected by the ignore rule, so a fresh clone runs the held-out test.
5. 2026-09-24 — Consequence for @k: from this commit on, ENC-A2 is in `main`'s history, so ANY bundle or clone of `main` contains it. Do not hand a `main` bundle to a reopened B1 chat without accepting that exposure. B1.4 already records a partial exposure and says an independent re-test after any future engine change needs a new held-out case.
6. 2026-09-24 — B4.2 step 6 (add ENC-A2 to the evaluation corpus) is NOT done: @k's I1 instructions say ENC-A2 goes nowhere else, including `fixtures/labelled_eval/`. The committed report and its hash are therefore unchanged.
7. 2026-09-24 — Step 3, read-only part, before asking @k: `evaluate --baseline v1 --verify` printed `APPROVAL_v1: consistent`; the rule set and registry sha256 equal the record (both files last changed 22 Sep, CCR-01/02); `evaluate --baseline v1 --candidate v1 --out-dir /tmp/eval` printed "gate passed; report sha256 81cc969e…" and its JSON is byte-identical (`cmp`) to B4's committed `fixtures/labelled_eval/reports/EVAL_v1_vs_v1.json`. Tier 1 recall 17/17. No file changed, so no hash needs refreshing. Approval waits for @k's written go-ahead; I1 does not approve.
8. 2026-09-24 — Observation for @k / B4 (not edited): `rulesets/APPROVAL_v1.md` prose opens with "**Status: DRAFT.** Not approved." My permission covers only three JSON fields (`status`, `approver_label`, `approved_on`), so after approval the prose would contradict the machine record (L19). B4 owns the record; @k decides who updates the prose, or authorises I1 to.
9. 2026-09-24 — Environment: Playwright 1.56.0 (B3's devDependency) wants chromium-1194, which is preinstalled in this container at `/opt/pw-browsers` (`PLAYWRIGHT_BROWSERS_PATH`); `npx playwright install chromium` exits 0 with nothing to download. No dependency or lockfile change. A fresh machine still needs `cd frontend && npx playwright install chromium`.
10. 2026-09-24 — Pre-swap baseline for step 7 (read-only run, no edit): `cd frontend && npm run e2e` on `main` 00cbe6b + the held-out commit → 12 passed (34.3 s), both projects (375 px and 1440 px). Recorded so any change after the swap can be attributed to the swap.
11. 2026-09-24 — Observation for @k (not edited; integrator-owned doc): `docs/contract_changes/CCR-03.md` still says "B3 part pending", while B3.2 reports the B3 part done (generated `StaleRevision` in use).
12. 2026-09-24 — FINDING (fallback): `create_app(engine=get_engine())` with NO `bundle_loader` does not run checks. `engine_seam.make_seam` then uses `_bundle_not_wired`, which raises NotImplementedError → 501 on every check run. The only ungated loader is `tests.support.golden.bundle()`, which its docstring reserves for tests. So the stated fallback ("swap the engine, pass no bundle_loader") is not implementable inside I1's paths: it needs a runtime ungated loader in B2's or B4's code, or @k's ruling. The approval path is the only clean route to CP2.
13. 2026-09-24 — FINDING (golden vs ruleset drift; no current test sees it): through the API, the real engine's OWN-001 `reason` on ENC-C1_1000 is `rulesets/v1.json`'s `reason_template` verbatim. B0's golden (`fixtures/expected/declarations.py` ~l.329) adds "on the care team" and names the attending (", Dr Lim,"). Why nothing caught it: the engine-level golden comparison leaves `reason` out of `FLAG_FIELDS`, and B2's full-JSON API comparison ran on the STUB, which serves the golden itself. Options for @k: (a) a CCR aligning the golden text to the template (golden-only; ruleset hash and approval unaffected); (b) change the template (B1), which changes the v1.json hash, so re-evaluate and re-hash APPROVAL (B4) and the freeze moves. I1 recommends (a). Nothing edited.
14. 2026-09-24 — Scratch preview (NOT a test, NOT a claim; B2 #29 precedent). The step-5 drafts are in `tests/e2e/test_api_real_engine.py`, UNCOMMITTED (they are red until approval), with a copy in outputs. They were run through a /tmp pytest plugin that swaps the gated loader for the tests-only bundle.
    - Gated (as the drafts are written): every test 503 `ruleset_unapproved`, so the gate is effective.
    - Ungated, golden scenarios: ENC-A1_1130, ENC-A1_1600_rerun, ENC-A1_1600 and ENC-B1_1600 match in full (exact flag set and every non-run-specific field, must-not-flag, bubbles, glance, summary with claims as a set, closure with blockers as sets). ENC-C1_1000 fails only on #13.
    - Ungated, grounding through the API: passed. CRIT-001 routing: passed.
    - Ungated, count integrity: the 5 scenarios and the post-`accept` step pass. The draft's `resolve` step is a DRAFT BUG (422: the first open Tier 1 is ALG-001, which needs `allergy_entry_confirmed` plus `adjudicated_evidence`, as in B2's test_lessons_api l.228).
    - Ungated, PDF-001 on a failed extraction: the draft fails at its source-text assertion; cause not read yet. No finding is claimed.
    Mutation spot-checks (12.1): NOT applied yet.
15. 2026-09-24 — The drafts use the `e2e` marker (`make e2e` is I1's), so B2's `make test-api` count is unchanged.
16. 2026-09-24 — Step 6 (Playwright smoke spec) not drafted yet.
17. 2026-09-24 — @k (chat): option (a) for #13; ruleset and registry FROZEN (step 2); "I approve of v1 on 24 September 2026", and I1 may change the record's status, which covers the prose status line from #8; B5 folds the decisions into DECISIONS.md (step 8), so I1 does not edit it.
18. 2026-09-24 — CORRECTION of what I1 told @k: option (a) does NOT leave the approval evidence untouched. `labelled.corpus_sha256()` hashes every golden JSON's bytes, so editing the golden changes the report hash. Landing CCR-05 therefore needs B4 to regenerate the report and refresh `evaluation_report_sha256`, and then @k's re-sign-off. The ruleset and registry hashes are unaffected. So I1 approved on the current evidence first (`--verify` consistent before and after the three fields) and did NOT land CCR-05; `docs/contract_changes/CCR-05.md` records the landing plan.
19. 2026-09-24 — FINDING for B1 via @k (B2's handoff item): PDF-001 is NOT raised on a `failed` extraction with no page table. The source is in scope, status failed, text empty, no pages; the only PDF-001 cites the scanned PDF. Evidence: `test_pdf001_on_failed_extraction_without_page_table`, xfail(strict=True) with this reference, so it fails loudly once fixed. Likely cause: `completeness.unreadable` builds evidence from `ctx.gap_items(d)` (one anchor per page), and an empty page table yields none. Engine not edited. Gap: that run's bubbles show no `not_documented…`, but a scanned PDF is also in scope, so the failed source's own effect on absence scope is not isolated.
20. 2026-09-24 — Mutation spot-checks (12.1), each applied then reverted with `git checkout` (tree verified clean), 4/4 caught:
    - M-I1-1: PEND-001 removed from the engine's rule table → golden test fails.
    - M-I1-2: the glance counts only `open` Tier 1 → count-integrity test fails.
    - M-I1-3: the store serves the source text shifted by one → grounding test fails.
    - M-I1-4: `tier1_owner` returns "" → routing test fails.
21. 2026-09-24 — Step 4 (B2's file; I1 checklist item 2 sanctions it): `noteguard/api/app.py` module-level app = `create_app(engine=get_engine(), bundle_loader=api_bundle_loader())`. `create_app()` defaults and `engine_stub.py` untouched. Check:
    - committed-tree `make test`: 1 failed, 129 passed, 1 skipped (REAL 0);
    - `make run` + a manual check run: 200 with `private, no-store`;
    - server log: 0 note-text hits.
22. 2026-09-24 — Own-test expectation change (18.10): until CCR-05 lands, the golden API test expects OWN-001's `reason` to equal the ruleset template (read from v1.json). The golden text is the wrong expectation per @k's ruling. It covers one field of one rule; everything else stays exact. Delete the override when CCR-05 lands.
23. 2026-09-24 — Smoke step 2 wording (B0's skeleton docstring) corrected in the I1-owned file: the ALG-001 flag's `question` is null in the golden and the engine; "Which allergy entry is correct?" is the `q_allergy_which_correct` bubble. The smoke asserts the ALG-001 card (title + Tier 1) and the bubble by EXACT full-string match (L9: never a substring).
24. 2026-09-24 — Smoke design:
    - Own config `tests/e2e/playwright.config.ts`: desktop 1440 only; `reuseExistingServer: false`, so a stale stub server is never reused; output under the git-ignored `frontend/test-results/`.
    - Driven from pytest via `npx playwright` with `NODE_PATH=frontend/node_modules`. No new dependency: Playwright is B3's devDependency.
    - Playwright output goes to a git-ignored log, never into assertion text.
    - STATUS AT PAUSE: UNCOMMITTED and failing. Steps 1-3 and the accept half of step 4 pass (the accepted CRIT-001 still blocks closure); the walk stops waiting for the "Review" navigation button after the Closure view.
    - The driver's mutation is NOT applied yet. Copies are in outputs/i1_drafts/.
25. 2026-09-24 — Step 7, B3 walk AFTER the swap (cd frontend && npm run e2e; B3 spec not edited):  2 failed  10 passed (47.7s) . Failing tests: main path: runs, flags, evidence, source viewer, decision, closure, su; B3 expected its stub-specific assertions to change at CP2 (e.g. "not available in this build" after a decision). Reported for B3 via @k; before the swap the same walk was 12 passed (#10).
26. 2026-09-24 — FINDING #19 WITHDRAWN: it was I1's test error, not an engine defect. B1.5's patch (docs + one engine regression test, no engine change) is applied as B1's own commit (`git am -k`, author kept). I verified it through the gated real API:
    - Why my test saw no flag: it uploaded the failed PDF "now" and ran at the golden 2026-09-21 16:00 cutoff. B2 decision #8 (intake `version_time` = server clock) and the contract scope rule exclude that upload, correctly.
    - At a cutoff at or after the upload, PDF-001 is raised with one page-less gap anchor (`context.gap_items` falls back to `page=None`), and every absence answer lists the failed version as `failed`; none says not documented. My "likely cause" in #19 was wrong.
    - B1's other explanation (the harness wires the stub) did not apply to I1's test, which wires the real engine. It DOES apply to everything run through `tests.support.lanes.real_app()` / `any_app()` (B2's API suite, B0's count test): those exercise the API with the stub engine. The real-engine API evidence is `tests/e2e/test_api_real_engine.py`.
27. 2026-09-24 — PDF test fixed:
    - `xfail` removed; it now runs at cutoff = now, taken after the upload (never in the future: B2's store rule, `store.py` l.417).
    - It asserts the page-less anchor and that every absence scope contains the failed version.
    - M-I1-5 (drop the page-less fallback in `gap_items`) → fails (applied, reverted, tree clean).
    - `test_api_real_engine.py`: 5 passed.
28. 2026-09-24 — For B5 / the brief (B2 #8, restated because I1 tripped on it): intake is as-of. A source added now is in scope only for runs at a cutoff at or after its recording, so a run at a golden-day cutoff ignores new uploads. The smoke's intake step therefore shows intake working, not intake changing the 11:30 run.
29. 2026-09-24 — CCR-05 landing is on side branch `ccr-05` (1ecdecb, based on 4adb915), kept off `i1-integration` so this branch stays green. Step 1 (I1) is done; it is red by design: REAL 1 = B4's `test_approval_verify_and_offline_clis`, and `--verify` prints `report_not_reproducible`. The expected new report sha256 is ba5f0711… (I1 cross-check in /tmp); only `corpus.sha256` differs and every metric is unchanged. Steps 2 (B4), 3 (@k re-signs) and 4 (I1 sets the fields, then merge) are pending, as written in CCR-05.md.
30. 2026-09-24 — Step 6: the smoke is green (1 passed).
    - Fixes: view switches are scoped to the "Encounter views" tab bar with exact names (substring matches caught other buttons), and the reason code is selected by its combobox role (the radio labels also mention "reason code").
    - Mutations, applied and reverted (tree verified clean), 2/2 caught: approval record back to draft → fails at the run step; an accepted Tier 1 no longer blocking closure → fails at the blocker count.
    - The smoke covers desktop only. It now runs inside `make test` (~20 s extra) and needs Chromium, so the README must include `cd frontend && npx playwright install chromium` (B5).
31. 2026-09-24 — Step 9: fresh clone of `i1-integration` from a bundle (4beabb5). Caveats: same container (browsers preinstalled, caches warm), and not `origin/main`, which @k has not merged yet. `make setup` exit 0; `make test` exit 0: 136 passed, 1 skipped (not implemented 0, REAL 0; held-out running, not skipped); the clone stays clean.
32. 2026-09-24 — Session I1.2 (same chat, after the I1.1 handoff). B4.4's bundle: two commits fast-forwarding I1's 1ecdecb, touching B4-owned paths only (report json/md, APPROVAL_v1.md, B4 docs). Checks before recording anything:
    - the report sha256 recomputed from B4's committed report = ba5f0711…, byte-equal to I1's independent /tmp run;
    - only `corpus.sha256` differs from the previous report;
    - in APPROVAL_v1.md B4 changed only `evaluation_report_sha256`, `status` (to draft) and the prose status line; label and date untouched.
33. 2026-09-24 — Re-sign: @k wrote "here is B4s part and you can sign for me". I1 treated this as @k's written authorisation for CCR-05 step 3 and recorded it only after the checks in #32 held; the approver remains @k ("Synthetic demo approver") and I1 only records. The prose status line quotes the authorisation. `status` set to approved; no hash edited; label and date were already correct.
34. 2026-09-24 — CCR-05 LANDED: `ccr-05` merged into `i1-integration` (4700efa, --no-ff); CCR-05.md status set to landed. Results:
    - `--verify`: consistent;
    - `make test-governance`: 9 passed;
    - `make test`: 136 passed, 1 skipped (NI 0, REAL 0).
    I1's golden API test compares OWN-001's reason to the golden again (the override was removed in step 1).
35. 2026-09-24 — Fresh clone of `i1-integration` at 9e6791e (same-container caveat as #31): `make setup` exit 0; `make test` exit 0, 136 passed, 1 skipped (NI 0, REAL 0); `--verify` consistent; clone clean. Final line for @k and B5: "136 passed, 1 skipped", on `i1-integration`, not yet on `main`.
36. 2026-09-24 — Session I1.3 (same chat). B3.4's bundle: 4 commits fast-forwarding `i1-integration` 3acb78b, touching `frontend/` and B3 docs only. Contents:
    - the walk updated for the real engine;
    - `fakeApi` mirrors CP2;
    - the generated AggregateView/AggregateCell types (the B3 part of CCR-04);
    - one fix: an untouched default cutoff now sends the exact last recording instant. The input shows whole seconds rounded up, which could be up to 1 s in the future and get 422; this is the same B2 store rule I1 hit in #27.
    Rerun by I1 in this container (Playwright's own chromium, not B3's NG_CHROMIUM):
    - `make test-ui`: 30 passed; `npm run typecheck`: clean;
    - `make test`: 136 passed, 1 skipped (NI 0, REAL 0), including I1's smoke on the rebuilt frontend;
    - B3's walk, `npm run e2e -- --repeat-each=2`: 24 passed (12 tests × 2, at 375 px and 1440 px); no servers left running.
    The step-7 finding (#25) is closed.
37. 2026-09-24 — Fresh clone at 496739f (same-container caveat as #31): `make setup` exit 0; `make test` exit 0, 136 passed, 1 skipped (NI 0, REAL 0); `--verify` consistent; clone clean.
38. 2026-09-24 — Left for @k (integrator-owned docs, outside I1's path list): B3 #55 asks for the CCR-03 and CCR-04 status lines to be updated; CCR-03 was already noted in #11.
39. 2026-09-24 — Session I1.4 (same chat), at @k's request: the CCR-03 and CCR-04 status lines now say LANDED, citing the landing commits from `git log`. The B3 parts were checked in the tree: the frontend uses the generated `StaleRevision` and AggregateView/AggregateCell, and no local `StaleBody` or stand-in remains. Closes #11, #38 and B3 #55. Status lines only; nothing else in either CCR changed.
40. 2026-09-24 — Session I1.5 (same chat), at @k's request: CCR-01 and CCR-02 status lines now say LANDED. Evidence from `git log`: @k's merge 336267a ("Merge ccr-01-02: CCR-01, CCR-02, ENC-C1 sign-off", 23 Sep 2026) and @k's re-sign 73a85f8, which marks ENC-A1_1130, ENC-A1_1600 and ENC-A1_1600_rerun "re-signed after CCR-02" in declarations.py and REVIEW_SHEET.md. Only the stale "Awaiting @k merge" and "PENDING re-sign-off" clauses were replaced; B1's landing description is kept. All five CCRs now read as landed.

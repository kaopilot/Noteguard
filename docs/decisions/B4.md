# B4 decisions (append-only; newest last)

1. 2026-09-23 — Session B4.1 spanned three chat turns (tool limits). The bundle carried no tags; `main` was 850ac2c (after B1, B2, B3 merges), not `cp0`. B4 started from that commit.
2. 2026-09-23 — The approval loader re-reads and re-verifies the files on EVERY call, never caches: `TermRegistry.cues` / `dose_unit_canonical` are dicts inside frozen models, so a handed-out bundle can be edited in place; re-verifying means such an edit never reaches the next run (L13).
3. 2026-09-23 — API refusal = B2's own `ApiError(RULESET_UNAPPROVED)` (503, error code only), raised by `approval.api_bundle_loader()`. B2's store catches only NotImplementedError, so it propagates to B2's handler; no B2 edit needed.
4. 2026-09-23 — Refusals list EVERY reason (not_approved, evaluation_report_missing, ruleset/registry hash mismatch, version mismatch, record missing/invalid); tests isolate each with a positive control.
5. 2026-09-23 — The pinned version is `governance.approval.PINNED_VERSION = "v1"`; the approval record names the registry version. Changing the pin is a release; rollback = re-pin the previous approved version.
6. 2026-09-23 — An `approved` record must carry `evaluation_report_sha256` (Section 11 step 5); `ApprovalRecord` allows null, the loader does not.
7. 2026-09-23 — `rulesets/APPROVAL_v1.md` updated as a DRAFT with the current file hashes (after CCR-01/02) and the report hash `81cc969e…`. Still refused (`not_approved` only). @k approves at CP2 after I1 reruns the evaluation.
8. 2026-09-23 — Protected = `protected_floor` OR Tier 1 in the BASELINE ruleset; never inferred from term kinds (PEND-001 consumes analyte terms but is not protected in v1). A proposal cannot unprotect a rule.
9. 2026-09-23 — RETIRE_TIER3_RULE / ADJUST_TIER3_THRESHOLD are also refused on protected rules: DOSE-002 is Tier 3 and protected (medication class).
10. 2026-09-23 — ADD_SYNONYM on a cue kind other than `pending` / `allergy_keyword` is refused (`narrows_shared_registry`): negation, uncertainty, change, response_*, owner and timing cues can remove flags, and the registry is shared with protected rules. Term synonyms are allowed (they add detections). Such changes go through a hand-authored version and evaluation.
11. 2026-09-23 — Every proposal must target a new ruleset version; a pinned version is never edited.
12. 2026-09-23 — Offline noise heuristic defaults: min_surfaced 10, dismissal rate 0.5, reading floor 2000 ms (REVIEW_STANDARD_OBSERVATIONS §4.2: ~25 words at 2–3 words/s). Function parameters of an offline tool; the test changes min_surfaced and observes the output change.
13. 2026-09-23 — Dispositions are counted once per FLAG from its latest decision event: B2 emits one event per decision (usefulness None) plus one per usefulness post that copies the latest action, so per-event counting double-counts.
14. 2026-09-23 — "Surfaced" denominator: supplied counts if given, else flags with feedback, and the output names its basis (exposure bias, Section 11 step 7).
15. 2026-09-23 — Owner response time is not computable from FeedbackEvent (no flag created_at): reported as not_available. B2's aggregate rows have it; CCR-04 proposes a cell field.
16. 2026-09-23 — `evaluate.py` runs the REAL engine (B1 had landed). The stub engine on golden scenarios would score itself perfect by construction, so it is not used.
17. 2026-09-23 — Corpus in two gated parts reported separately: `b4_labelled` (independent of engine development) and `golden` (B0/@k signed; the engine was developed against it). Known-limit cases are run and reported, never gated.
18. 2026-09-23 — Labels were committed before any engine run on them (2f2da6b precedes evaluate.py in 76918bc). Stated limit: I read the registry before writing the cases, so they test a new roster and wording WITHIN registry vocabulary, not paraphrase.
19. 2026-09-23 — Scoring: match on (rule_id, subject_key; a trailing '*' is a prefix), greedy one-to-one; any unlabelled flag is a false positive (exact set, like the goldens). A Tier 1 label counts as caught only if raised at Tier 1.
20. 2026-09-23 — Gate refuses on: tier1_recall_below_1, tier1_recall_decreased, new_protected_miss, and static floor violations (protected rule removed/disabled/tier lowered/unprotected; registry synonyms or denials removed; widening cues removed; suppressing cues extended; critical/deterioration thresholds loosened).
21. 2026-09-23 — The report is deterministic (no wall clock); `evaluation_report_sha256` = sha256 of its canonical JSON (`ids.canonical_json`). Stored under `fixtures/labelled_eval/reports/`, a B4-owned path.
22. 2026-09-23 — B0's seam scanner reads dict LITERALS whose keys are log keys (`rule_id`, `tier`, `encounter_id`) as a redefined key list. Record dicts in labelled.py/evaluate.py are built with `dict(...)` calls instead. The scanner is unchanged and no key list is defined; report hash unchanged.
23. 2026-09-23 — Aggregate cells include every state (disposition visible through `state`). Primary small-cell suppression only ("<5"); complementary suppression is a declared gap. `ruleset_version` = the version pinned at generation time, because B2's rows carry no check_version.
24. 2026-09-23 — Aggregate route uses `response_model=None`: declaring AggregateView changes `docs/openapi.json` (integrator-owned) though the body is identical. CCR-04 proposes declaring it plus a response-time bucket.
25. 2026-09-23 — Strengthened B4's `test_aggregate_no_content_or_ids` (all B0 assertions kept): the original passed on an EMPTY view. Now populated by real runs, both count branches, 401, age-bucket boundaries, store layer.
26. 2026-09-23 — B2-owned `tests/api/test_api_behaviour.py::test_aggregate_route_is_b4_placeholder_behind_b2_authz` asserts the 501 placeholder (line 430) and fails now that B4 has landed. Not edited (18.4 rule 3). Proposed for B2/integrator: `assert r.status_code == 200 and "cells" in r.json()`.
27. 2026-09-23 — AI validator certainty uses the contract `Certainty` ordinal at two levels (registry uncertainty cue present = POSSIBLE, else ASSERTED); a sentence may not exceed the least certain quote it cites. Added a polarity check (a negated sentence must cite a negated quote) beyond Section 9's list. No new word lists: all vocabulary comes from the registry and `forbidden_phrases`.
28. 2026-09-23 — `Drafter.status_view()` reports only observed outcomes: disabled; breaker open -> unavailable; enabled with no draft yet -> unavailable_fallback + "no draft observed yet" (AIDraftStatus has no ENABLED value; a CCR would add one). Never "validated" without a validated draft (L10).
29. 2026-09-23 — No route calls the drafter (the contract has only /api/ai/status). B2's constant "disabled" is truthful while disabled; proposed B2 change: `ai_status()` returns `app.state.ai_drafter.status_view()` with a default `Drafter()` (identical output today).
30. 2026-09-23 — AI tests use B2's REAL redactor (`get_redactor`, landed); no fake Redactor exists anywhere.
31. 2026-09-23 — No new dependency (stdlib `concurrent.futures` for the timeout). A timed-out worker thread runs until the provider returns (declared, like B2's PDF timeout).
32. 2026-09-23 — Process slip: commit 76918bc left the seam test red on the lane branch because the commit chain gated on grep's exit status. Fixed in 5ccefba; later commits gate on pytest's exit code.
33. 2026-09-23 — Observation for B1/I1 (no change made): on a rerun with prior flags (`ENC-A1_1600_rerun`) the engine keeps a flag's earlier tier after the ruleset re-tiers the rule. Safe for a lowered tier; a RAISED tier would not reach existing flags.
34. 2026-09-23 — Mutation spot-checks, each applied then reverted, all caught (13/13): M1 status check removed; M2 feedback path clears registry cues in place; M3 cached loader + feedback disables a rule (caught by the content hash, not the flag set, because the engine keeps the prior flag); M4 is_protected -> False; M5 per-event disposition counting; M6 tier ignored in Tier 1 recall; M7 suppressing-cue floor check removed; M8 small counts shown; M9 B2's store-layer aggregate check removed; M10 age-bucket boundary shifted; M11 no timeout on the provider result; M12 validator entity check removed; M13 validator certainty check removed.
35. 2026-09-23 — Session B4.2 (same chat as B4.1): double-check pass requested by @k. Verified: branch touches only B4-owned paths (+ CCR-04); both offline CLIs executed on files (never run before); frontend suite 24/24 unaffected.
36. 2026-09-23 — `evaluate.verify_approval` / `--verify`: an approved record must match its files, the committed report, and a FRESH rerun whose gate passes. The test enforces this on the real record only once it is approved, so a draft may lag during integration without turning main red.
37. 2026-09-23 — L11 applied to B4's offline parameters. `noisy_dismissal_rate`, `reading_floor_ms`, the aggregate `threshold` and the loader `version` each now have an assertion that changes them and observes the output change (min_surfaced and every AIConfig field already had one).
38. 2026-09-23 — Frontend observation (B3's path, not edited): a check run refused for an unapproved ruleset shows "Checks did not run (server code ruleset_unapproved)"; a 503 on a derived read (bubbles/summary call the loader) would be shown as an outage with retry. I1 must approve v1 before, or together with, wiring the loader.
39. 2026-09-23 — Section 11 items NOT built, now declared in the brief part: dismissal-spike monitoring against baseline (step 6), a blind sample of non-flagged sources (missed concerns, step 2), shadow-only learned ranking (ranking stays deterministic). The exposure-bias statement (step 7) was missing from the brief part and is added.
40. 2026-09-23 — Mutation M14 (drop the fresh-rerun comparison in verify_approval) applied and caught. The new L11 assertions were not mutation-checked one by one: each is itself a changed input with an observed output change.

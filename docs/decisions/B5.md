# B5 decisions (append-only; newest last)

1. 2026-09-24 — Session B5.1 is B5's first session. It starts from `main` eff8608 (after I1 / CP2), not `cp0`, on a new branch `b5-docs`, context file r4. Baseline `make setup && make test`: setup exit 0; `136 passed, 1 skipped, 1 warning in 45.67s`; tally NI 0 · REAL 0 · errors 0. This is identical to I1's final line, so nothing was stopped.
2. 2026-09-24 — `NOTEGUARD_BUILDER_BRIEF.md` was missing from the first B5.1 turn, and B5 did not reconstruct it. @kaopilot supplied it in the second turn. The trace covers:
   - its §1–8 requirements (49 rows) and its nine required proofs, which equal the appendix micro-tests 1–9;
   - the appendix items beyond it;
   - build-context lessons L1–L20.
3. 2026-09-24 — Owner handle is @kaopilot (his instruction). It is applied in every file B5 writes or finalises: README, TECHNICAL_BRIEF, REQUIREMENTS_TRACE, PLAN, DECISIONS and the B5 files. It is NOT applied to other lanes' records (18.4, append-only), to goldens and review sheets, to the held-out package or to `rulesets/APPROVAL_v1.md`:
   - `fixtures/expected/ENC-A1_1600.json` contains "@k", and B4's `corpus_sha256` hashes every golden JSON's bytes, so an edit changes the evaluation report hash and un-verifies APPROVAL_v1 (the CCR-05 chain).
   - The held-out README and review sheet are covered by the pinned manifest hash.
   - A full rename is an integrator decision: a CCR-05-style commit with B4's report refresh, a re-sign and a manifest re-pin.
   - `docs/DECISIONS.md` carries one line mapping "@k" to @kaopilot.
4. 2026-09-24 — Evidence levels in the trace:
   - E = executed in B5.1's own runs (R1 `make test`, R2 per-test JUnit, R3 `--verify`, R4 browser walk, R5 fresh clone);
   - E-lane = executed by a lane outside `make test`, quoted from its handoff;
   - I = inspected;
   - NP = not produced (reason).
   Every API row carries the caveat that B2's suite runs through `real_app()` with the stub engine (I1 #26). The real engine through the API is evidenced by I1's five tests and the smoke test.
5. 2026-09-24 — Measured check-run latency (scratch script outside the repo; printed status codes and timings only, never a body, L8).
   - Setup: `noteguard.api.app:app` (real engine, approval gate); ENC-A1 at the 16:00 golden cutoff; fresh login and workspace per run; only the POST timed with `time.perf_counter`; n=100 after 5 warm-ups; in-process TestClient; 1 vCPU, x86_64, Python 3.12.3.
   - Run 1: median 26.7 ms, p95 36.2 ms, max 69.1 ms. Run 2: median 25.7 ms, p95 28.5 ms, max 72.5 ms. All 200 with 6 flags.
   - B2's 9.1 ms was measured with the STUB engine and is not quoted as product latency.
6. 2026-09-24 — B3's browser walk run by B5: `cd frontend && npm run e2e` gave 12 passed (40.8s), exit 0, at 375 px and 1440 px (Playwright's Chromium at /opt/pw-browsers). A first attempt started in the background was killed when the tool call returned (empty log, no result claimed). One server left listening after the walk was stopped before the fresh-clone run.
7. 2026-09-24 — Trace integrity check (scratch, not committed; B5 owns no test path). Every `test_*` name cited in the trace (68) exists in the collected suite, and every one passed in R2 except `test_stub_serves_goldens`, which the trace cites as the deliberate skip. Every cited test file exists.
8. 2026-09-24 — B5 wrote no test, so no mutation spot-check was due, and none was applied. No dependency was added.
9. 2026-09-24 — Brief parts corrected rather than copied:
   - B3's "no feedback UI / no aggregate page" gaps are stale (both built in B3.3).
   - B4's "v1 back to draft" is superseded (re-signed, I1 #33).
   - B0's "50 not implemented" is now 0.
   - B2's latency is stub-engine.
   Numbers kept only where a handoff records the command or method.
10. 2026-09-24 — FINDING, trace P5 partial. "A note author does not automatically gain access" holds by design: authorisation reads care-team membership, never authorship. No test shows it:
    - in every fixture the authors are a subset of the care team;
    - API intake only accepts care-team authors;
    - the access tests use a clinician on no team and a clinic admin.
    Proposal for B2/@kaopilot: a store-level test with a source authored by a non-member (the EMR-adapter case) asserting 404, or a static check that `authz`/`store` never read `author_staff_id`. Not written by B5 (not B5's path).
11. 2026-09-24 — Observation for @kaopilot (root file, not edited). `THIRD_PARTY_NOTICES.md` presents the care-core MIT notice with "Copyright (c) 2019 Sebastián Ramírez" (FastAPI's author) and "Copyright (c) 2026 Nightingale contributors". B5 cannot see care-core's LICENSE; please check the notice against it.
12. 2026-09-24 — Observation: `make setup`'s `npm ci` reports 2 moderate advisories. B3 #3 records the vitest ≤ 4.1.10 dev-only advisory; B5 did not re-audit.
13. 2026-09-24 — TECHNICAL_BRIEF length: 2,678 words including two text diagrams and two tables (about 2,500 outside code blocks), roughly four pages. That is at the appendix's 2–4 page ceiling, after two trimming passes that dropped no traced claim.
14. 2026-09-24 — PLAN records that B5 started after CP2 instead of drafting from CP0 (as the schedule planned), and lists the cuts actually made. "Workstation three-pane" is recorded as a side panel on wide screens, not verified by B5 as three panes.
15. 2026-09-24 — Session B5.2 checked I1.6 (a9b9f45, "@k" → "@kaopilot" throughout, on top of B5.1 90f7d1b); `b5-docs` fast-forwarded to it. Checks, all executed:
    - every change outside `docs/` is a comment, docstring or prose line, with no assertion or logic changed (word diff of noteguard/, tests/, frontend/, rulesets/, fixture scripts);
    - `APPROVAL_v1.md` changed only in its prose, not its JSON record;
    - no pinned input changed (0 files among the rulesets, golden JSON, labelled corpus, held-out package, encounters, PDFs, OpenAPI, contract data, generated TS);
    - the held-out `MANIFEST.sha256` equals its original pin (bc478fbf5be0…, set at b302ecb), and `sha256sum -c` gives 8/8 OK;
    - `--verify` prints consistent; `make goldens` leaves the tree byte-identical;
    - `make test` on a9b9f45: 136 passed, 1 skipped, 1 warning in 51.48s (NI 0, REAL 0, errors 0).
    I1.6's own claims (decisions/I1.md #41) all reproduce.
16. 2026-09-24 — The blanket replace also rewrote sentences ABOUT the rename in B5's files, making them circular or false: PLAN ("call the owner "@kaopilot""), brief part ("keep "@kaopilot" (= @kaopilot)"), #3 above ("contains "@kaopilot"", which is false because the golden contains "@k"), the B5.1 handoff, and a DECISIONS bullet contradicting I1's new line. Fixes:
    - PLAN, the brief part and DECISIONS were rewritten to the current truth.
    - In #3 and the B5.1 handoff, only the quoted tokens were restored to "@k", because they quote the literal string. #3's "not applied" is superseded by I1.6; the history is kept, not rewritten.
17. 2026-09-24 — Two hyphenated mentions the regex missed (`@k-signed` in `docs/brief_parts/B1.md`, owner B1, and `docs/kickoff/B1.md`, owner integrator) were renamed under @kaopilot's instruction to change the handle. They are in a separate, separable commit; neither file is hash-pinned.
18. 2026-09-24 — "@k" remaining by design:
    - the ENC-A1_1600 golden note (the JSON, its declarations line, the review-sheet line), which is in the approved evaluation corpus;
    - `fixtures/heldout/` (4 lines, manifest-pinned);
    - sentences that quote the old token (DECISIONS handle section, I1 #41 and the I1.6 handoff, B5 #3 and #15–18, the B5.1 handoff).
    Git commit messages are unchanged.

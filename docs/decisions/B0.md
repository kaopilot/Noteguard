# B0 decisions (append-only)

1. 2026-09-22 — Evidence and assertion spans are **statement spans** defined once in `contracts/spans.py` — exact-offset goldens need one boundary rule shared by engine, goldens and UI. Known limit: "e.g. " splits a statement.
2. 2026-09-22 — Added route `GLANCE` — `GlanceView` existed with no endpoint.
3. 2026-09-22 — Existing flags keep citing the version they cited while the normalised facts hold; a newer in-scope version sets `source_changed_since_flag` and bumps `revision` (not `evidence_revision`) — implements 8.4 and feedback 16 without losing the citation.
4. 2026-09-22 — Added a third golden, `ENC-A1_1600_rerun` (prior flags supplied) — the live app reruns on every source change, so superseded Tier 1 and source-changed markers must be pinned, not only the fresh cutoffs.
5. 2026-09-22 — Flag reasons, questions, uncertainty notes and claim text are linted, not compared exactly — wording is B1's; everything clinically significant is exact.
6. 2026-09-22 — **Governance question for @k:** SpO2 89% / RR 26 do not raise CRIT-001 in v1 (registry marks them deterioration markers feeding DIFF-001/DET-001, not critical values). Listed as must-not-flag with reason.
7. 2026-09-22 — An amendment keeps its source's `source_time` (medrec v2 stays at 10:30), so the 15:30 metformin change is later and produces no DOSE-001.
8. 2026-09-22 — Evidence roles fixed (README convention 4); ALG-001 `claim` = denial side regardless of order.
9. 2026-09-22 — Subject keys: registry key; `source:<id>` for PDF-001; `status:stable@source:<id>` for DIFF-001; `unparsed_dose@source:<id>:<sha16(quote)>` for DOSE-002.
10. 2026-09-22 — `documented` bubbles carry the trigger plus qualifying matching statements (for `q_crit_response`, exactly the 8.5 suppressors).
11. 2026-09-22 — Adjudication references evidence spans (`EvidenceRef`), replacing `adjudicated_assertion_ids` — clients never see assertion ids, so "this entry is correct" was not expressible. Pre-CP0 change.
12. 2026-09-22 — Froze the redaction/egress seam in `contracts/egress.py` (B2 ↔ B4), ported from care-core `egress.py` (MIT); B2 exposes `noteguard.redaction.get_redactor()`.
13. 2026-09-22 — Froze PDF upload form fields (`AddPdfSourceForm`, file part `file`).
14. 2026-09-22 — `decision_target` / `engine_target` accept wire strings and always raise `InvalidTransition` for anything invalid — found by `test_engine_never_targets_human_states` (AttributeError would have surfaced as a 500).
15. 2026-09-22 — Stub limits, stated: no authorisation, `CheckRunResult.assertions/changes` empty from the stub engine, `run_id`/`evaluated_at` ignored, 501 for intake/decisions/documents/feedback/aggregate; it does require the session cookie and workspace header and never echoes input.
16. 2026-09-22 — Test mechanism: lane gates (`tests/support/lanes.py`) raise `NotImplementedError` until a lane lands; B2 tests refuse the stub; B1 test bodies are complete; internal B2/B4 behaviours are placeholders carrying the spec in their docstring; `conftest.py` tallies not-implemented vs real failures.
17. 2026-09-22 — `test_access_control` ends with a `not_implemented` fault-injection step B2 must replace with its store API.
18. 2026-09-22 — Skip (own test): `test_heldout_golden` until I1 installs ENC-A2; `test_stub_serves_goldens` retires itself once the real API is in place.
19. 2026-09-22 — Registry v1 seed gained six generic terms (bisoprolol, atorvastatin, sulfonamides class, sulfonamide allergen, urine culture, chest X-ray) for the held-out case; APPROVAL_v1.md draft hashes refreshed. It stays **draft**, so B4's runtime must refuse it: @k must approve a ruleset before the demo can run checks.
20. 2026-09-22 — ENC-A2 is delivered outside the repo (`noteguard_heldout_ENC-A2.zip`); only its manifest hash is committed (`fixtures/heldout/README.md`); `fixtures/heldout/*` is git-ignored.
21. 2026-09-22 — Dependencies (B0.1, all via uv.lock): fastapi MIT, pydantic MIT, uvicorn BSD-3-Clause, pdfplumber MIT, python-multipart Apache-2.0, pytest MIT, httpx BSD-3-Clause, hatchling MIT. None added since.
22. 2026-09-22 — **Proposal, not decided (needs a CCR before B1 builds OWN-001):** `Flag.evidence` requires ≥1 item but OWN-001 ("no responsible clinician") has no text span to cite. Options: (a) add `EvidenceRole.ENCOUNTER_RECORD` with a zero-length anchor on the earliest source; (b) relax `min_length` for OWN-001 only. No golden covers OWN-001 yet.
23. 2026-09-22 — Stretch rules DET/LAT/META/NEXT stay disabled in v1; enabling one needs a CCR with golden expectations.
24. 2026-09-22 — The starlette TestClient DeprecationWarning (anyio alias) is upstream and harmless.
25. 2026-09-22 — @k confirmed the OPEN-1…8 defaults.
26. 2026-09-22 — @k decided #6: SpO2/RR stay a DIFF-001 question in v1; DET-001 is the first stretch rule if time allows, only via a CCR adding its goldens. The golden's must-not-flag text now records the decision.
27. 2026-09-22 — @k decided #22: OWN-001 cites the care-team field, not a note. Contract: `EvidenceRole.ENCOUNTER_RECORD`, `RecordField`, `Evidence.record_field`; `note_version_id` is null only for that kind (validator). New fixture ENC-C1 (no responsible clinician; Tier 1 routes to the attending) and golden `ENC-C1_1000` — **pending @k review**.
28. 2026-09-22 — @k signed off the four ENC-A1/ENC-B1 scenarios ("the review sheet seems fine"), recorded as `SIGNOFF` in declarations.py and printed per scenario on the review sheet. Changes to them after sign-off: the additive `record_field: null` on every evidence item and the SpO2 wording from #26 (verified by diff; no flag, bubble or claim changed).
29. 2026-09-22 — Generated TypeScript for B3: `frontend/src/api/schema.gen.ts` (openapi-typescript 7.13.0, MIT, run pinned via npx, `--enum-values`) and `frontend/src/api/contract-data.gen.ts` (from `docs/contract_data.json`: routes, headers, error statuses, decision rules, permission matrix). Owner B0/integrator; B3 never edits them. `test_generated_types` fails when an input changed without `make types` (mutation-checked). Lint and seam test exempt `*.gen.ts`.
30. 2026-09-22 — Held-out ENC-A2: removed "For medical review." from the 09:40 note before sign-off (request-vs-response is ambiguous, and a held-out failure must mean overfitting, not ambiguity); package and manifest hash regenerated.
31. 2026-09-22 — Tier 1 owner with no responsible clinician = attending (8.6); materializer and golden invariants check "RC, else attending".
32. 2026-09-22 — Kickoff prompts for B1–B5 and an I1 checklist in `docs/kickoff/`. Work moves between chats as git bundles; @k integrates.

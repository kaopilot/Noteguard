# B3 handoffs (newest first)

## Session B3.1 — 23 Sep 2026, ~11:40–12:20 SGT (container clock) — context file r4
Branch: b3-ui @ 0cffe16 for the code (docs commit follows; see `git log -1`). Base: main 8eb506b (after the B2 merge).

Built:
- `frontend/` React 19 + TypeScript + Vite PWA over B2's API:
  - Login picker (synthetic roster), workspace, reset case (hidden when `encounter_ids` is empty), sign-out, offline banner.
  - Glance strip.
  - Run control (Singapore-time cutoff).
  - Timeline with diff chips and `! contradicts`.
  - Flag cards (tier label + shape, owner, state, markers, evidence, history) with a hide-only decision sheet:
    - reason codes from `reasons_by_action` ∩ `resolve_reasons_by_rule`;
    - adjudication picker;
    - duplicate picker;
    - 409 message naming who decided first.
  - Source viewer (verified code-point spans, cited and current versions side by side, PDF banner and document token).
  - Question bubbles, closure view with "Check closure now", summary with copy and print.
- `public/sw.js` (shell only, never `/api/`), `offline.html`, manifest, icons.
- `frontend/tests` (Vitest, 13 cases) and `frontend/e2e/walk.spec.ts` (Playwright, 375 px and 1440 px).
- `tests/ui/test_ui_frontend.py` replaces the placeholders.

Tests:
- `make test`
  - Summary line: `8 failed, 116 passed, 2 skipped, 1 warning in 12.44s`
  - Tally: not implemented 7 · REAL 1 (`tests/governance/test_governance.py::test_aggregate_no_content_or_ids`, B4's, known before this session) · errors 0.
  - Baseline before B3 work: 13 failed, 110 passed, 2 skipped; NI 12 · REAL 1 (same test) · errors 0.
- `uv run pytest -m ui -q` → 6 passed.
- `make test-ui` (npm test) → 13 passed.
- `cd frontend && NG_CHROMIUM=/opt/google/chrome/chrome npx playwright test` → 6 passed (~21 s). Not in `make test`: it needs a browser and two servers.

Owned tests:
| Test | Result | Status |
|---|---|---|
| test_sw_never_caches_api | pass | executed (Vitest via pytest) |
| test_offset_conversion_utf16 | pass | executed (117 golden spans + the emoji case) |
| test_no_clinical_data_in_browser_storage | pass | executed (jsdom main path + static scan); also executed in the real-browser walk |
| test_tier_labels_not_colour_only | pass | executed |
| test_no_dangerously_set_inner_html | pass | executed |
| test_frontend_suite_all_green | pass | executed (includes the stale-409 case) |

Mutation spot-checks: 8/8 caught, plus the pytest wrapper (docs/decisions/B3.md #17).

Main path at 375 px and 1440 px: executed in Chromium 141 against B2's API with the stub engine.
- Executed: 11:30 and 16:00 runs (2 then 3 Tier 1); superseded CRIT-001 shown with its confirm prompt; ALG-001 "source changed since flag" with cited v1 beside current v2; the 16:00 "Patient stable" highlight after an emoji; summary; accept on DOSE-001; closure blocked; refusal for Dr Kaur; offline shell. Storage after the path is empty and the SW cache holds shell paths only.
- Also executed once in a scratch real-engine server outside the repo: diff chips render, and the post-decision glance works ("Dr Lim accepted: Dose differs between sources").
- Inspected only:
  - Decision paths other than accept: edit, reassign, dismiss, resolve with adjudication, mark ready.
  - Print layout.
  - Clipboard copy.
  - PWA installability.
- No focus trap in the sheet.
- The carried-forward chip does not open the origin beside the later note.

Not built:
- Add-source UI (paste text, PDF upload); the API supports both.
- Feedback (usefulness) UI.
- Aggregate page.
Each is absent from the UI, not faked.

Contract: CCR-03 raised (proposed). It exports the `StaleRevision` 409 body to openapi/schema.gen; local structural stub until then (decisions #12). No integrator-owned file touched: Makefile, contracts, goldens and `*.gen.ts` are all unchanged.

Known gaps:
- The stub engine gives no diff chips, and bubbles, glance and summary answer 501 after any decision. The UI shows both states explicitly, and the walk asserts the 501 states, so those assertions change at CP2.
- Vitest 3.2.7 dev-only advisory (decisions #3).

Next step:
1. After @k merges b3-ui, act on CCR-03: replace `StaleBody` with the generated `StaleRevision`.
2. Ask @k whether the add-source UI (paste/PDF) belongs to B3: I1's smoke names "multidisciplinary intake".
3. Extend `e2e/walk.spec.ts` to resolve-with-adjudication, dismiss-with-reason, reassign and mark-ready against B2's API.
4. At CP2, re-run the walk on the real engine and replace the stub-specific 501 assertions with chip and post-decision glance checks.

# B3 handoffs (newest first)

## Session B3.4 — 24 Sep 2026, ~01:50 SGT (container clock) — context file r4
(Same chat. @k: "the main-path walk fails at both screen sizes after the swap, 2 of 12".)

Branch: b3-ui @ 68ce0bd for the code (docs commit follows). Base: `i1-integration` 3acb78b (I1.2).

Changed:
- `frontend/e2e/walk.spec.ts`: stub-specific assertions replaced with real-engine ones (decisions #52).
- `frontend/tests/fakeApi.ts` mirrors CP2, with stub 501s opt-in; one new not-built test.
- `api/types.ts`: generated `AggregateView`/`AggregateCell` (CCR-04 B3 part).

Tests:
- `make test`
  - Summary line: `136 passed, 1 skipped, 1 warning in 43.69s`
  - Tally: not implemented 0 · REAL 0 · errors 0.
- Browser walk (`cd frontend && NG_CHROMIUM=/opt/google/chrome/chrome npx playwright test`) → 12 passed (~46 s), real engine, 375 px and 1440 px.
- `make test-ui` → 30 passed; also 30/30 on Node 26.
- Mutations: 3/3 caught (decisions #56).

Owned tests: all six → pass → executed.

For @k: update the CCR-03 and CCR-04 status lines (decisions #55).

Next step: optional polish only, if time allows before release (decision-path walk, focus trap, axe in the repo, carried-forward origin, "Sign out" on one line). B5's README should list running the UI and Node 22/24/26.

## Session B3.3 — 23 Sep 2026, ~23:18 SGT (container clock) — context file r4
(Same chat. @k approved CCR-04 and assigned the feedback UI to B3.)

Branch: b3-ui @ 03ee048 for the code (docs commit follows). Base: main 894ada7 + B3.2.

Built:
- Feedback UI on decided flags: usefulness, optional corrected owner, measured time on screen or null, one answer per flag per person per workspace.
- Aggregate page for governance roles: counts as served, "<5" kept, no totals, no identifiers. It uses a local stand-in type until CCR-04 part 1 lands.

Tests:
- `make test`: see the summary line in the B3.3 commit message.
- `make test-ui` → 29 passed.
- Browser walk → 12 passed (~46 s), Node 22.
- Mutations: 7/7 caught (decisions #50).

Owned tests: all six → pass → executed; `test_frontend_suite_all_green` now covers 29 cases.

Executed against real code: B2's feedback route (201) and B4's aggregate route (200) in the browser walk.

Not built: the "time to first decision" column shows only when B4 fills `response_time_bucket` (CCR-04 part 2).

CCRs:
- CCR-04 part 1 is for @k: `response_model=AggregateView` in B4's `noteguard/api/routes/aggregate.py`, then `make types`.
- CCR-04 part 2 is for B4: the contract field, filling it, and its test.
- B3 then replaces the stand-in type (decisions #48).

Next step, in order:
1. @k: merge; land CCR-04 part 1.
2. B3: swap the stand-in for the generated types (about 10 minutes).
3. B3: the B3.1 list (decision-path walk, focus trap, axe in the repo, carried-forward origin, "Sign out" on one line).

## Session B3.2 — 23 Sep 2026, ~22:43 SGT (container clock) — context file r4
(Same chat as B3.1: updates requested by @k after B4's first pass. B4's chat was read for items addressed to B3.)

Branch: b3-ui @ cd31f04 for the code (docs commit follows). Base: main 894ada7 (B4 + B2.3 + B2.4).

Built:
- Governance pause: 503 `ruleset_unapproved` on runs, questions, glance and summary shows "… paused: the rule set is awaiting clinical governance approval." It is never labelled an outage.
- AI status wording aligned with B4's drafting module.
- CCR-03 B3 part: the generated `StaleRevision` is used for the 409 message.

Tests:
- `make test`
  - Summary line: `1 failed, 127 passed, 2 skipped, 1 warning in 20.85s`
  - Tally: not implemented 1 (`tests/e2e/test_e2e_smoke.py`, I1) · REAL 0 · errors 0. Baseline identical (decisions #37).
- `make test-ui` → 25 passed; also 25/25 on Node 26.
- Browser walk → 10 passed (~41 s), Node 22.
- Mutations: 4/4 caught (decisions #44).

Owned tests: all six → pass → executed (unchanged from B3.1; `test_frontend_suite_all_green` now covers 25 cases).

Executed against real code:
- B4's approval loader wired as I1 will (scratch server, draft record): the paused copy appears and no outage banner.

Not built:
- Aggregate page: blocked on CCR-04 (decisions #42).
- Feedback UI: ownership with @k (decisions #43).

CCRs: CCR-03 B3 part done, ready to be marked landed. CCR-04 still proposed (B4's).

Known gaps: unchanged from B3.1 (see below), minus CCR-03.

Next step, in order:
1. @k: merge this branch.
2. @k: rule on CCR-04 and on feedback-UI ownership.
3. B3.3:
   - the aggregate page (if CCR-04 is approved) and the feedback UI (if assigned);
   - the B3.1 list: decision-path walk, focus trap, axe in the repo, carried-forward origin, "Sign out" on one line.
4. At CP2 (I1): once the ruleset is approved and wired, re-run the walk. The stub-specific 501 assertions change then, and the paused state should no longer appear.

## Session B3.1 — 23 Sep 2026, ~11:40–16:39 SGT (container clock) — context file r4
Branch: b3-ui @ c7fd0a1 for the code (docs commit follows; see `git log -1`). Base: main 8eb506b (after the B2 merge).

Built:
- `frontend/` React 19 + TypeScript + Vite PWA over B2's API:
  - Login picker (synthetic roster), workspace, reset case (hidden when `encounter_ids` is empty), sign-out, offline banner.
  - Glance strip.
  - Run control: Singapore time, seconds precision; defaults to "everything recorded so far", rounded up to the second.
  - Timeline with diff chips and `! contradicts`.
  - Flag cards (tier label + shape, owner, state, revision, check version, markers, evidence, history).
  - Decisions: inline in the card on wide screens, bottom sheet on mobile, hide-only:
    - reason codes from `reasons_by_action` ∩ `resolve_reasons_by_rule`;
    - adjudication and duplicate pickers;
    - 409 names who decided first.
  - Source viewer (verified code-point spans; cited and current versions side by side; PDF banner; original via document token; explicit state for a missing version).
  - **Add a source** (added this session at @k's request): paste or PDF, new source or new version (metadata locked), care-team authors, idempotent retries, explained refusals.
  - Question bubbles ("AI drafting disabled").
  - Closure view (blockers with owner, age and evidence link) with "Check closure now".
  - Summary with copy and print.
  - Outage handling: last results kept and labelled stale on 5xx or no network, never on 4xx.
- `public/sw.js` (shell only, never `/api/`), `offline.html`, manifest, icons.
- `frontend/tests` (Vitest, 24 cases) and `frontend/e2e/walk.spec.ts` (Playwright, 5 tests × 375 px and 1440 px).
- `tests/ui/test_ui_frontend.py` replaces the placeholders.

Tests:
- `make test`
  - Summary line: `8 failed, 116 passed, 2 skipped, 1 warning in 17.95s`
  - Tally: not implemented 7 · REAL 1 (`tests/governance/test_governance.py::test_aggregate_no_content_or_ids`, B4's, known before this session) · errors 0.
  - Baseline before B3 work: 13 failed, 110 passed, 2 skipped; NI 12 · REAL 1 (same test) · errors 0.
- `uv run pytest -m ui -q` → 6 passed.
- `make test-ui` (npm test) → 24 passed.
- `cd frontend && NG_CHROMIUM=/opt/google/chrome/chrome npx playwright test` → 10 passed (~37 s) on Node 22.22.2, 24.21.0 and 26.9.0. Not in `make test`: it needs a browser and two servers.
- `npx vitest run` → 24 passed on Node 22, 24 and 26 (decisions #34).

Owned tests:
| Test | Result | Status |
|---|---|---|
| test_sw_never_caches_api | pass | executed (Vitest via pytest; plus the runtime cache inspected in the real-browser walk) |
| test_offset_conversion_utf16 | pass | executed (117 golden spans + the emoji case) |
| test_no_clinical_data_in_browser_storage | pass | executed (jsdom main path + static scan; also in the walk, including after intake) |
| test_tier_labels_not_colour_only | pass | executed |
| test_no_dangerously_set_inner_html | pass | executed |
| test_frontend_suite_all_green | pass | executed (all 24 cases, including intake, outage/403, stale 409, inline decisions, cutoff precision) |

Mutation spot-checks: 22/22 caught, plus the pytest wrapper (decisions #17, #25, #32).

Executed in Chromium 141 at 375 px and 1440 px against B2's API with the stub engine:
- Main path, as before: two runs, superseded Tier 1, v1 beside v2, emoji-offset highlight, summary copy and print, accept, closure blocked, Kaur refusal, offline shell, installability, PDF via one-time token (reuse → 404), scanned-PDF state, empty storage, SW cache shell-only.
- Intake: paste (emoji + Chinese), new version of Ward round, both fixture PDFs (text and scanned) through B2's real PDF intake, non-PDF refused (415 text).
- With the stub engine, a run at a non-golden cutoff answers 501, and the UI says "not available in this build".
- Scratch real-engine server (outside the repo), through the UI: an added note is read by B1's engine ("≈ reworded: amlodipine", cited by DOSE-001).
- axe-core 4.13.0: 0 violations in 14 states (scratch run; the add-source sheet was not in that scan).

Inspected only:
- Decision paths other than accept.
- Edge and Safari: not available in this container; see decisions #31.
- No focus trap in the mobile sheets.
- The carried-forward chip does not open the origin beside the later note.

Not built:
- Feedback (usefulness) UI.
- Aggregate page.
Each is absent from the UI, not faked.

Contract: CCR-03 raised (proposed). No integrator-owned file touched: Makefile, contracts, goldens and `*.gen.ts` are all unchanged.

Known gaps; where B3 fails first:
- The previous bundle had a phone-layout regression (decisions #33); this bundle fixes it. If `b3-ui` was fetched earlier, fetch again before merging.
- On phones, "Sign out" may wrap onto two lines (cosmetic).
- Running the UI needs `cd frontend && npm run dev` beside `make run`; B5 owns documenting it or adding a target (@k's decision).
- Stub-engine limits: no chips, 501 after any decision, and 501 for runs at non-golden cutoffs. The walk asserts these, so those assertions change at CP2.
- Vitest 3.2.7 dev-only advisory.
- For B2: the `sources_in_scope` log count can overstate the engine's scope (decisions #29).

Gap plan (who closes what):
- **B3, next session (~2 h):**
  - Walk the other decision paths (edit, reassign, dismiss, resolve with adjudication, mark ready) against B2.
  - Add a focus trap to the mobile sheets.
  - Add axe-core as a dev dependency and include the add-source sheet in the scan.
  - Show the carried-forward origin beside the later note.
  - Keep "Sign out" on one line on phones without widening the layout (run the walk after any CSS change).
  - Feedback (usefulness) UI, if @k confirms B3 owns it (the API route exists).
- **Waiting on other lanes:**
  - CCR-03 (B2 + integrator), then replace `StaleBody`.
  - Aggregate page after B4's endpoint stops returning 501.
  - Stub-specific assertions after I1 (CP2).
- **Needs a person on a real machine (B5 or @k):**
  - The walk in Edge and Safari (latest two majors).
  - Retry vitest ≥ 4.1.11 after CP1.
- **B5:** make the UI runnable from the README (or a make target).

Next step: after @k merges b3-ui, start the next B3 session with the "B3, next session" list above, beginning with the decision-path walk.

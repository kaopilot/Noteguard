# B3 handoffs (newest first)

## Session B3.1 — 23 Sep 2026, ~11:40–12:40 SGT (container clock) — context file r4
Branch: b3-ui @ e487754 for the code (docs commit follows; see `git log -1`). Base: main 8eb506b (after the B2 merge).

Built:
- `frontend/` React 19 + TypeScript + Vite PWA over B2's API:
  - Login picker (synthetic roster), workspace, reset case (hidden when `encounter_ids` is empty), sign-out, offline banner.
  - Glance strip.
  - Run control (Singapore-time cutoff).
  - Timeline with diff chips and `! contradicts`.
  - Flag cards (tier label + shape, owner, state, revision, check version, markers, evidence, history).
  - Decisions: **inline in the card on wide screens, bottom sheet on mobile**, hide-only:
    - reason codes from `reasons_by_action` ∩ `resolve_reasons_by_rule`;
    - adjudication and duplicate pickers;
    - 409 names who decided first.
  - Source viewer (verified code-point spans; cited and current versions side by side; PDF banner; original via document token; explicit state for a missing version).
  - Question bubbles ("AI drafting disabled").
  - Closure view (blockers with owner, age and evidence link) with "Check closure now".
  - Summary with copy and print.
  - Outage handling: last results kept and labelled stale on 5xx or no network, never on 4xx.
- `public/sw.js` (shell only, never `/api/`), `offline.html`, manifest, icons.
- `frontend/tests` (Vitest, 18 cases) and `frontend/e2e/walk.spec.ts` (Playwright, 375 px and 1440 px).
- `tests/ui/test_ui_frontend.py` replaces the placeholders.

Tests:
- `make test`
  - Summary line: `8 failed, 116 passed, 2 skipped, 1 warning in 14.77s`
  - Tally: not implemented 7 · REAL 1 (`tests/governance/test_governance.py::test_aggregate_no_content_or_ids`, B4's, known before this session) · errors 0.
  - Baseline before B3 work: 13 failed, 110 passed, 2 skipped; NI 12 · REAL 1 (same test) · errors 0.
- `uv run pytest -m ui -q` → 6 passed.
- `make test-ui` (npm test) → 18 passed.
- `cd frontend && NG_CHROMIUM=/opt/google/chrome/chrome npx playwright test` → 8 passed (~37 s). Not in `make test`: it needs a browser and two servers.

Owned tests:
| Test | Result | Status |
|---|---|---|
| test_sw_never_caches_api | pass | executed (Vitest via pytest; plus the runtime cache inspected in the real-browser walk) |
| test_offset_conversion_utf16 | pass | executed (117 golden spans + the emoji case) |
| test_no_clinical_data_in_browser_storage | pass | executed (jsdom main path + static scan; also in the real-browser walk) |
| test_tier_labels_not_colour_only | pass | executed |
| test_no_dangerously_set_inner_html | pass | executed |
| test_frontend_suite_all_green | pass | executed (includes the stale-409, outage, 403, missing-version, inline-decision and Questions→source cases) |

Mutation spot-checks: 13/13 caught, plus the pytest wrapper (docs/decisions/B3.md #17, #25).

Executed in Chromium 141 at 375 px and 1440 px against B2's API with the stub engine:
- 11:30 and 16:00 runs (2 then 3 Tier 1); superseded CRIT-001.
- ALG-001 cited v1 beside current v2; the 16:00 "Patient stable" highlight after an emoji.
- Summary; clipboard copy (starts with the human-review statement and carries citations); print layout.
- Accept on DOSE-001; closure blocked.
- PDF original through the single-use `no-store` token (reuse refused); scanned PDF shows its no-text state.
- Refusal for Dr Kaur; offline shell.
- Installability (only Playwright's `in-incognito` error remains; manifest parses clean).
- Storage empty after the path; SW cache holds shell paths only.
- axe-core 4.13.0: 0 violations in 14 states after two fixes (scratch run, not in the repo).
- Once, in a scratch real-engine server outside the repo: diff chips render and the post-decision glance works.

Inspected only:
- Decision paths other than accept: edit, reassign, dismiss, resolve with adjudication, mark ready.
- Safari: no WebKit available here; Review Standard R12 lists Safari. Edge shares Chromium's engine but was not run as Edge.
- No focus trap in the mobile sheet.
- The carried-forward chip does not open the origin beside the later note.

Not built:
- Add-source UI (paste text, PDF upload); the API supports both.
- Feedback (usefulness) UI.
- Aggregate page.
Each is absent from the UI, not faked.

Contract: CCR-03 raised (proposed). It exports the `StaleRevision` 409 body to openapi/schema.gen; local structural stub until then. No integrator-owned file touched: Makefile, contracts, goldens and `*.gen.ts` are all unchanged.

Known gaps; where B3 fails first:
- **No make target runs the UI** (`make run` is API only). Until @k decides, run `cd frontend && npm run dev` beside `make run`. See decisions #26.
- The stub engine gives no diff chips, and bubbles, glance and summary answer 501 after any decision. The UI shows both explicitly; the walk asserts the 501s, so those assertions change at CP2.
- Vitest 3.2.7 dev-only advisory (decisions #3).

Next step:
1. After @k merges b3-ui, act on CCR-03: replace `StaleBody` with the generated `StaleRevision`.
2. Get @k's answer on the UI make target and on whether the add-source UI belongs to B3 (I1's smoke names "multidisciplinary intake").
3. Extend the walk to the other decision paths against B2's API, and add axe-core as a dev dependency so the a11y scan repeats.
4. At CP2, re-run the walk on the real engine and replace the stub-specific 501 assertions.

# B1 handoffs (newest first)

## Session B1.5 — 23 Sep 2026, start/end times not recorded — context file r4
Branch / last commit SHA: b1-engine, cut from main at 00cbe6b (delivered as a patch for main).
Trigger: @k reported "PDF-001 isn't raised on a PDF whose extraction failed completely", with the suggested cause
"the engine builds PDF-001 evidence from the page table, and a failed extraction has no page table".

Finding: NOT REPRODUCIBLE in the engine; no engine change made. Evidence, in order:
  1. Engine directly, golden ENC-A1 at 16:00 with the scan's extraction replaced by status=failed, pages=():
     PDF-001 IS raised, evidence = one extraction_gap with page unset, start == end == 0, quote "".
     The gap builder already falls back to a page-less anchor when there is no page table (context.gap_items).
  2. Through the API with the REAL engine wired (create_app(engine=get_engine(), bundle_loader=...)), uploading an
     unparseable PDF (intake gives status failed, page_count 0) and running at cutoff = now: PDF-001 count 2
     (the seeded scan and the new one); the new one's anchor has page null.
  3. Through the API as the test harness wires it (tests.support.lanes.real_app): PDF-001 count 1 and a non-golden
     cutoff returns 501. real_app() uses noteguard.engine_stub.StubEngine, which serves golden fixtures only, so an
     uploaded source can never produce a flag there. This is the most likely origin of the report.
  4. Scope is as-of (18.3): a document uploaded today has version_time = today, so a run "as of" an earlier cutoff
     (e.g. the 16:00 golden) correctly excludes it. A run at cutoff = now includes it.
  Also observed: rulesets/APPROVAL_v1.md is still "draft", so B4's api_bundle_loader refuses with 503
  ruleset_unapproved; the real engine cannot be exercised through the API until that is approved (CP2).

Built this session: tests/engine/test_engine_edges.py::test_pdf_001_when_extraction_failed_without_page_table
  (B1-owned regression test pinning the behaviour), RULES.md PDF-001 entry (null page anchor + as-of scope note).

Last test command run, verbatim, and its summary line:
  $ make test-engine
  =========== 49 passed, 1 skipped, 81 deselected, 1 warning in 1.16s ============
  $ make test
  ============= 1 failed, 128 passed, 2 skipped, 1 warning =============
  tally: not implemented (lane not landed yet): 1 · REAL failures: 0 · setup/teardown errors: 0
  (the 1 is I1's e2e smoke test; unchanged from the baseline at session start)

Tests owned → status → evidence level:
  test_pdf_001_when_extraction_failed_without_page_table → pass → executed. Mutations: build gap evidence only from
    the page table -> fails; skip sources whose extraction has no page table -> fails.
  All other B1 tests → pass → executed.

Not built or cut, with reason: no engine change; the reported cause was not the engine's behaviour.

Contract change requests raised: none.

Known gaps; where this lane fails first: unchanged (docs/RULES.md). For other lanes, from this investigation:
  - B3: a failed-extraction gap anchor carries page = null; the UI must not assume a page number.
  - B2/I1: real_app() still wires the stub engine; anything tested through it reflects the stub, not the engine.
  - @k/B4: APPROVAL_v1.md is "draft", so the real engine is unreachable through the API (503) until CP2 approval.

Next step (the first thing the next session should do): if @k still sees a missing PDF-001 after wiring the real
engine and running at a cutoff at or after the upload, ask for the extraction status, page_count, source_time,
version_time and the run cutoff; those five values decide which of the four cases above applies.

## Session B1.4 — 23 Sep 2026, start/end times not recorded — context file r4
(Same chat as B1.1-B1.3. @k merged ccr-01-02 into main, re-signed ENC-A1, and ran the held-out ENC-A2.)
Branch / last commit SHA: main (this note arrives as a patch @k applies on main, because main holds @k's merge and
re-sign commits that B1's branches lack); b1-engine is fast-forwarded to main by @k.

Built this session: no engine or rule change. Recorded the held-out result and an exposure note; brief part updated.

Last test command run, verbatim, and its summary line (run by @k on main after the merge; output shared in chat):
  $ make test
  27 failed, 72 passed, 1 skipped, 1 warning
  tally: not implemented (lane not landed yet): 27 · REAL failures: 0 · setup/teardown errors: 0
  $ NOTEGUARD_HELDOUT_DIR=<package dir> uv run pytest tests/engine/test_heldout_golden.py -v
  2 passed, 1 warning   (after @k's answer-key update below)

Tests owned → status → evidence level:
  test_heldout_golden x2 (owner I1) → pass → executed by @k, not by B1. First run: ENC-A2_1200 passed; ENC-A2_1730
    flags had 0 missing and 1 extra, a DET-001 flag (the answer key predated CCR-02). @k checked it against the
    approved rule, added the DET-001 entry to ENC-A2's answer key, and the rerun passed both scenarios in full
    (flags, bubbles, summary). No engine change.
  All other B1 tests → pass → executed (@k's make test above).

Not built or cut, with reason: LAT-001, META-001, NEXT-001 (disabled in v1, not built); per-source version Change
  records; Summary.stale_after_source_change is set by the caller, never the engine.

Contract change requests raised: none new (CCR-01 and CCR-02 landed via ccr-01-02).

Known gaps; where this lane fails first:
  - EXPOSURE: the first ENC-A2 run's full output was pasted into this B1 chat, so B1 saw two ENC-A2 17:30
    statements (one status line, one observation line) and their ids. No engine change followed. Treat ENC-A2
    as partially seen by B1; a fully independent re-test after any future engine change needs a new held-out.
  - Everything listed per rule in docs/RULES.md ("Known" lines).
  - CP2: rulesets/v1.json and registry_v1.json changed after APPROVAL_v1.md was drafted (CCR-01, CCR-02); B4
    refreshes the hashes after its evaluation.

Next step (the first thing the next session should do): the B1 lane is complete for v1. If it is reopened: pull
main, run make test-engine, and treat any I1 or held-out finding as a pattern to generalise, never content to key on.

## Session B1.3 — 22 Sep 2026, start/end times not recorded — context file r4
(Same chat as B1.1/B1.2. @k approved CCR-01 and CCR-02 with defaults and ENC-C1 in chat B1.2, and asked B1 to
prepare the landing as one branch.)
Branch / last commit SHA: ccr-01-02 / see `git log -1` (this note is in the last [B1.3] commit). The lane branch
b1-engine is unchanged at 8cae742; ccr-01-02 = b1-engine + the commits below, so merging ccr-01-02 lands both.

Built this session (one commit per owner/step, so @k can review or drop each):
  - ada7958 CCR-01 contract (INTEGRATOR-OWNED): `TermRegistry.dose_unit_canonical` + validator (every mapped and
    canonical unit must be in `dose_units`, factor > 0). `make types` regenerated identical files.
  - 58d8daf CCR-01 B1 content: the table in `registry_v1.json`; the engine reads it through the Lexicon;
    `noteguard/engine/units.py` deleted.
  - 2b28e88 CCR-02 landing (INTEGRATOR-OWNED goldens + B1 rule content, one commit as the CCR requires):
    DET-001 `enabled: true`; the DET-001 declarations exactly as approved (ENC-B1/ENC-C1 optional lines omitted);
    `make goldens`; ENC-A1_1130/1600/1600_rerun set back to PENDING sign-off; B1 tests updated for the approved
    behaviour (decisions/B1.md #36).
  - f109c31 ENC-C1 sign-off (INTEGRATOR-OWNED, optional): "@k, 22 Sep 2026 (approved in chat B1.2)".
  - Docs: CCR-01/02 statuses, RULES.md, decisions/B1.md #32-37, brief part, this note.

Last test command run, verbatim, and its summary line:
  $ make test
  ============= 27 failed, 72 passed, 1 skipped, 1 warning in 1.71s ==============
  tally: not implemented (lane not landed yet): 27 · REAL failures: 0 · setup/teardown errors: 0
  (none of the 27 is in tests/engine or tests/contract)
  $ make test-engine
  =========== 48 passed, 1 skipped, 51 deselected, 1 warning in 0.89s ============
  Baseline at session start (b1-engine 8cae742): the same two lines.

Tests owned → status → evidence level:
  test_golden_engine x5 → pass → executed; now includes DET-001 at ENC-A1 16:00 (fresh and rerun). Checked by
    script: outside DET-001 every golden JSON is byte-identical to CP0's; closure status unchanged in all five.
  test_det_001 x11 → pass → executed. Mutation "DET-001 enabled: false" -> goldens and test_det_001 fail.
  test_cross_note_conflicts → pass → executed. Mutations: drop g->mg from the registry table -> fails; an
    unregistered unit in the table -> the validator refuses the registry at load (tests fail).
  All other B1 tests → pass → executed (unchanged). Mutation totals across B1.1-B1.3: 34 applied, 30 caught first
    time, the 4 misses explained and closed (brief part).
  test_heldout_golden → skipped → not produced (ENC-A2 never given to B1).

Not built or cut, with reason:
  - ENC-A1 re-sign-off: only @k can do it (read REVIEW_SHEET.md, restore the three SIGNOFF lines).
  - Approval hashes in rulesets/APPROVAL_v1.md not refreshed: both ruleset files changed; CP2 step (B4/@k).
  - Held-out ENC-A2 answer key not checked for a DET-001 entry: B1 must not read it.
  - Still open: per-source version Change records; engine never sets Summary.stale_after_source_change.

Contract change requests raised: none new. CCR-01 and CCR-02 approved; landing prepared, awaiting @k merge.

Known gaps; where this lane fails first: as B1.1/B1.2 (RULES.md). Other lanes: B2/B3 see one more Tier 1 flag in
  the ENC-A1 16:00 goldens (same flag shape; no wire change); the stub engine serves the new goldens.

Next step (the first thing the next session should do): if @k merged ccr-01-02, rebase b1-engine on main and run
make test; then triage the ENC-A2 result if one exists (general patterns only, never ENC-A2 content). If not
merged, ask @k which commits to change or drop.

## Session B1.2 — 22 Sep 2026, start/end times not recorded — context file r4
(Same chat as B1.1, continued after the B1.1 bundle was delivered.)
Branch / last commit SHA: b1-engine / see `git log -1` (this note is in the last [B1.2] commit). B1.1 ended at f3ca462.

Built this session:
  - Opening ritual: baseline equal to B1.1's close (below). The B1.1 next step's first two items were not
    available in this chat (no I1 ENC-A2 result, no decision on CCR-01), so this session did the third: DET-001.
  - DET-001 (reassurance after deterioration, Tier 1) in `noteguard/engine/rules/deterioration.py`, registered in
    the rule dispatch, BUILT BUT DISABLED: rulesets/v1.json is unchanged, so no golden and no approval hash moved.
  - CCR-02 (proposed): enable DET-001; hand-written proposed golden declarations for ENC-A1_1130/1600/1600_rerun,
    ENC-B1, ENC-C1; five semantic questions for @k.
  - tests/engine/test_det_001.py (B1-owned; enables DET-001 in an in-memory bundle only).
  - RULES.md DET-001 entry; decisions/B1.md #26-31; brief part updated.

Last test command run, verbatim, and its summary line:
  $ make test
  ============= 27 failed, 72 passed, 1 skipped, 1 warning in 1.66s ==============
  tally: not implemented (lane not landed yet): 27 · REAL failures: 0 · setup/teardown errors: 0
  (none of the 27 is in tests/engine)
  $ make test-engine
  =========== 48 passed, 1 skipped, 51 deselected, 1 warning in 0.80s ============
  Baseline at session start: make test 27 failed, 61 passed, 1 skipped (not implemented 27, REAL 0);
  make test-engine 37 passed, 1 skipped.

Tests owned → status → evidence level:
  Everything listed under B1.1 → still pass → executed (re-run this session).
  test_det_001_cases x9, test_det_001_shape_owner_and_supersede, test_det_001_disabled_in_v1_and_proposed_golden
    → pass → executed. Mutations (applied, run, reverted): any author counts as clinician review; drop the
    "at or before the reassurance" bound; carried review counts; marker need not precede reassurance; review of
    any analyte counts; queried reassurance counts -> all 6 caught. The first mutation was NOT caught until the
    nurse case was rewritten so its response cue shares a clause with SpO2 (decisions/B1.md #31).
  test_heldout_golden → skipped → not produced (ENC-A2 never given to B1).

Not built or cut, with reason:
  - DET-001 is not ENABLED: needs @k's answer and sign-off on CCR-02 (golden change).
  - LAT-001, META-001, NEXT-001: not built (disabled in v1; enabling raises NotImplementedError).
  - Still open from B1.1: per-source version Change records; engine never sets Summary.stale_after_source_change.

Contract change requests raised:
  - CCR-02 (proposed): enable DET-001 + goldens. CCR-01 (proposed, B1.1) still awaits a decision.

Known gaps; where this lane fails first:
  - As B1.1 (RULES.md "Known" lines). DET-001 adds: a general "obs reviewed" names no analyte and does not answer
    it; a recovered observation does not clear it; the ENC-A1 16:00 line would carry DET-001 and DIFF-001 together.
  - When CCR-02 lands, held-out ENC-A2 goldens may need DET-001 entries (I1, outside B1's view).

Next step (the first thing the next session should do): same order as B1.1's — (1) triage the I1 ENC-A2 result if
@k has it, generalising rules only; (2) act on CCR-01 and CCR-02 decisions (for CCR-02: flip `enabled` in
rulesets/v1.json in the SAME merge as the integrator's golden update, and adjust the rule if @k picked an
alternative in questions 1-5); (3) otherwise, per-source version Change records (8.3), which B3's diff chips can use.

## Session B1.1 — 22 Sep 2026, start/end times not recorded (one chat, resumed once after a tool limit) — context file r4
Branch / last commit SHA: b1-engine / see `git log -1` (this note is in the last [B1.1] commit; the engine code
it describes is at 36257b4). Base: cp0 (c5f32b9).

Built this session:
  - Opening ritual (no earlier B1 handoff, decisions or open CCR). Baseline recorded below.
  - `noteguard/engine/`: registry index compiled only from the bundle; normalisation with offset map;
    extraction on contract statement spans with a NegEx-style negation/uncertainty scope; differencing
    (carried_forward / explicit_change / reworded / new); rules CRIT-001, ALG-001, DOSE-001, DOSE-002,
    PEND-001, PDF-001, DIFF-001, OWN-001; flag orchestration (identity via contracts.ids, one disagreement =
    one flag, owner routing 8.6, rerun merge keeping citations + source_changed_since_flag, supersede /
    reopen via contracts.states, adjudication L2); bubbles with absence scoping; summary builder.
    `get_engine()` returns the real engine (`is_stub = False`).
  - docs/RULES.md (one entry per enabled rule + the four disabled ones), docs/decisions/B1.md (#1-25),
    docs/brief_parts/B1.md, docs/contract_changes/CCR-01.md.
  - tests/engine/test_engine_edges.py (new, B1-owned: 13 edge cases + one non-ENC-A1 variant encounter).
  - tests/engine/test_lessons_engine.py::test_adjudication_converges made STRICTER (restate the winning
    side -> no reopen), see decisions/B1.md #24.

Last test command run, verbatim, and its summary line:
  $ make test
  ============= 27 failed, 61 passed, 1 skipped, 1 warning in 1.67s ==============
  tally: not implemented (lane not landed yet): 27 · REAL failures: 0 · setup/teardown errors: 0
  (none of the 27 is in tests/engine; all are other lanes' "not implemented")
  $ make test-engine
  =========== 37 passed, 1 skipped, 51 deselected, 1 warning in 0.78s ============
  Baseline at session start: make test 50 failed, 24 passed, 1 skipped (not implemented 50, REAL 0);
  make test-engine 23 failed, 1 skipped.

Tests owned → status → evidence level (mutation = applied, run, reverted with git; scripts were scratch):
  test_golden_engine x5 (incl. ENC-A1_1600_rerun, ENC-C1_1000) → pass → executed; exact set equality.
    Mutation: evidence in flag identity -> fails (rerun golden). ENC-C1 golden is still pending @k review.
  test_grounding x5 → pass → executed. Mutation: UTF-8 byte offsets instead of code points -> fails.
    (The docstring's "normalised offsets" mutation is NOT observable on this fixture: normalisation
    preserves length here.)
  test_critical_observation_routing → pass → executed. Mutations: drop "later", drop carried-forward
    check, let a negated response suppress, suppress on any analyte -> each fails.
  test_cross_note_conflicts → pass → executed. Mutation: compare raw doses (no unit canonicalisation) -> fails.
  test_pending_owner_and_time → pass → executed. Mutations: owner OR timing; ignore linked statements -> fail.
  test_differencing → pass → executed. Mutations: ignore change cues; drop "contradiction in between" -> fail.
  test_question_bubble_grounding → pass → executed. Mutation: absence ignores extraction status -> fails.
  test_conflict_fanout_one_flag (L1) → pass → executed. Mutation: one flag per evidence pair -> fails.
    Evidence-in-id is NOT caught by this test (its rerun has identical evidence); the rerun golden catches it.
  test_adjudication_converges (L2) → pass → executed. Mutations: ignore the adjudicated entry -> fails (only
    after the added restate step; the original test did not catch it); never reopen -> fails.
  test_change_not_contradiction (L3), test_unparsed_dose_never_passes (L4), test_single_term_registry (L5),
    test_tier1_never_auto_closed, test_cited_source_edited_side_by_side, test_engine_is_pure
    → pass → executed; each failed under its docstring mutation.
  test_engine_edge x13, test_variant_encounter_generalises → pass → executed (2 mutations caught). Same lane
    as the engine: a regression guard, not independent evidence.
  test_heldout_golden → skipped → not produced (ENC-A2 is never given to B1; I1 runs it).
  Totals: 25 mutations applied; 22 caught first time; 3 not caught at first (two weak mutations, caught when
    re-run as intended; one real L2 test gap, now closed).

Not built or cut, with reason:
  - DET-001, LAT-001, META-001, NEXT-001: disabled in v1 and not built; enabling one makes run_checks raise
    NotImplementedError. DET-001 only via a CCR with goldens (@k, 22 Sep); not started.
  - Per-source version diffs (8.3 "per source", EMR amendments) do not emit Change records; amendments show
    as source_changed_since_flag on flags. No `removed` changes are emitted.
  - Summary.stale_after_source_change is always False from the engine (only the caller sees later sources).

Contract change requests raised:
  - CCR-01 (proposed): dose-unit canonical forms into the TermRegistry schema; until then the arithmetic lives
    in noteguard/engine/units.py (unknown units compare as written, so they can only flag, never pass).

Known gaps; where this lane fails first (details per rule in docs/RULES.md):
  - Regex/dictionary extraction: paraphrase and anything outside the registry is not seen.
  - Negation window is shallow (clause, 6 tokens, stops at ":").
  - CRIT-001: a response written in the same note as the critical value does not suppress (8.5 read strictly;
    false positive). Responses that name no analyte never suppress.
  - PEND-001 associates subject and pending cue per statement (false positives on mixed statements).
  - Reruns assume the cutoff only moves forward (a finding that disappears is superseded).
  - answer_bubbles/build_summary re-extract per call (27 ms median for the triple on ENC-A1; fine at demo scale).
  - rulesets/*.json are UNCHANGED, so the approval hash needs no refresh for B1's work.
  - Commits use a lane-local git identity (decisions/B1.md #25).

Next step (the first thing the next session should do): ask @k for the I1 result of the held-out ENC-A2
(`NOTEGUARD_HELDOUT_DIR=<pkg> make test-engine`); fix any failure by generalising a rule, never by keying on
ENC-A2 content, and record each fix in decisions/B1.md. If none is available yet: act on CCR-01's outcome,
then DET-001 only through a CCR that adds its goldens.

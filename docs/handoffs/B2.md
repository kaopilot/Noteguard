# B2 handoffs (newest first)

## Session B2.3 — 23 Sep 2026 — context file r4
Branch / last commit SHA: b2-api / see `git log -1`. Base: main ecd7b67 (after the B3 and B4 merges).
Built this session: fixed the one REAL failure on main. After B4 replaced the aggregate 501 placeholder,
  B2's aggregate test is renamed `test_aggregate_route_behind_b2_authz` and now asserts:
  - 401 without a session; 403 for a clinician and a clinic admin;
  - 200 with a valid AggregateView for the medical director and quality/risk;
  - with the route dependency removed, the store still refuses (fault injection).
  Mutation caught. Decisions #35.
Last test command run, verbatim, and its summary line:
  $ make test-api
  ================= 39 passed, 88 deselected, 1 warning in 3.42s =================
  tally: not implemented (lane not landed yet): 0 · REAL failures: 0 · setup/teardown errors: 0
  $ make test
  ============= 1 failed, 124 passed, 2 skipped, 1 warning in 16.88s =============
  tally: not implemented (lane not landed yet): 1 (I1's e2e) · REAL failures: 0 · setup/teardown errors: 0
  Baseline at session start (main ecd7b67):
    make test: 2 failed, 123 passed, 2 skipped (NI 1 · REAL 1 = this test)
    make test-api: 1 failed, 38 passed (REAL 1)
Tests owned → status → evidence level: all 39 B2 API tests → pass → executed; mutation spot-checks 22/22 caught.
Not built or cut, with reason: nothing new was asked. Pending items waiting on @k are listed in decisions #36:
  - CCR-03;
  - B4's optional ai_status suggestion;
  - B3's `sources_in_scope` log note;
  - the stub-engine 501s, which are I1's engine swap.
Contract change requests raised: none.
Known gaps; where this lane fails first: unchanged from B2.2.
Next step (the first thing the next session should do): if @k approves CCR-03, add the 409 `responses=` declaration and run `make types`; otherwise support I1's engine swap.

## Session B2.2 — 23 Sep 2026, ~02:00–09:30 SGT (container clock) — context file r4
(Same chat as B2.1.)
- A double-check pass after the first bundle added two tests and fixed one flaky test (decisions #30).
- @k's rulings are recorded in #31–33.
- The aggregate data seam for B4 was added (#34).
Branch / last commit SHA: b2-api / see `git log -1` (this note is in the last [B2.2] commit). Base: main dd698aa.
Commits (each separable):
- lane code: api, intake, redaction and tests/api.
- make types output (INTEGRATOR-OWNED): openapi.json and schema.gen.ts.
- THIRD_PARTY_NOTICES line (@k may drop it).
- docs.

Built this session:
- Real API on exactly ROUTE_TEMPLATES; `app.state.is_stub = False`; `routes/stub.py` removed.
- Synthetic login with a random server-side session token (HttpOnly, SameSite=Lax cookie).
- Two-layer authz: route guard over the seed directory, plus the store gate over the workspace copy.
- Per-page-load workspace store seeded with ENC-A1/B1/C1; token only in X-Workspace-Token.
- Intake:
  - Paste and PDF.
  - PDF limits, magic bytes, timeout, and the pinned pdfplumber extraction.
  - Checksum, idempotency key and versioning.
- Check-run through one engine seam (stub by default), with an engine-result guard.
- Decisions with compare-and-swap, reason codes and 8.7 field rules.
- Closure view and closure attempt, glance, summary with the stale marker, bubbles.
- Single-use document tokens.
- Feedback capture.
- Allowlist JSON logger with route-template request log; uvicorn access log off; noisy third-party loggers capped.
- Error handlers (error_code only; bulk_not_supported); no-store, nosniff and no-referrer headers.
- Hash-chained audit.
- `noteguard.redaction` with `get_redactor()`, `prepare_egress()` and a labelled eval set.
- `routes/aggregate.py`: 501 behind `authz.require_aggregate_viewer`, handed to B4.
- Tests:
  - All B2 placeholders filled.
  - New tests/api/test_api_behaviour.py: golden views through the API, intake, PDF, documents, closure, decision rules, marker test, sessions, engine guard, feedback, redaction, L11 matrix.

Last test command run, verbatim, and its summary line:
  $ make test-api
  ================= 39 passed, 86 deselected, 1 warning in 4.73s =================
  tally: not implemented (lane not landed yet): 0 · REAL failures: 0 · setup/teardown errors: 0
  $ make test
  ============= 13 failed, 110 passed, 2 skipped, 1 warning in 6.57s =============
  tally: not implemented (lane not landed yet): 12 · REAL failures: 1 · setup/teardown errors: 0
    FAILED tests/governance/test_governance.py::test_aggregate_no_content_or_ids  (B4-owned; KNOWN until B4 lands, @k ruling #33)
  Baseline at session start (dd698aa):
    make test: 27 failed, 72 passed, 1 skipped (NI 27 · REAL 0)
    make test-api: 13 failed, 1 passed (NI 13)

Tests owned → status → evidence level (all executed; mutation spot-checks 21/21 caught, decisions/B2.md #26, #30, #34):
  test_access_control (incl. store + HTTP fault injection) → pass
  test_store_layer_authz_fault_injection (L6/L7) → pass
  test_audit_allowlist (L8) → pass
  test_no_unread_config_keys (L11) + test_config_key_changes_behaviour x6 → pass
  test_no_env_conditional_security (L12) → pass
  test_decision_requires_reason_code, test_no_bulk_decisions (L15) → pass
  test_role_matrix_staff_prepare_clinician_close (L16) → pass
  test_validation_error_no_echo (L20) → pass
  test_concurrent_decision_409 → pass
  test_no_store_headers → pass
  test_log_and_redaction_safety → pass
  test_pdf_extraction_boundary → pass
  test_api_behaviour.py: 20 tests (incl. concurrent decisions, unhandled-exception 500, aggregate seam) → pass
  B0 tests B2 keeps green → pass:
    test_registered_routes_equal_contract
    test_error_bodies_are_error_code_only
    test_openapi_export_in_sync
    test_open_count_integrity (against the REAL app now)
    test_generated_types
  test_stub_serves_goldens → skipped (retired as designed)

Not built or cut, with reason:
  - Nothing from card B2 cut.
  - Not built:
    - aggregate view (B4's; 501 placeholder);
    - feedback export route (none in the contract; B4/I1 read `store.feedback_events`);
    - AI status stays a constant "disabled" (B4 owns the module).

Contract change requests raised: none. Integrator-owned files touched in separate commits:
  - docs/openapi.json and frontend/src/api/schema.gen.ts via `make types`, as the kickoff instructs. The diff is additive, listed in decisions/B2.md #23.
  - THIRD_PARTY_NOTICES.md: one line (#28).

Known gaps; where this lane fails first:
  - MERGE NOTE (@k ruling #33): after B2 lands, B4's test_aggregate_no_content_or_ids is one KNOWN real failure (501 != 200) until B4 replaces the placeholder. B2 did not edit it.
  - With the stub engine, bubbles/summary/glance return 501 after any decision (explicit); I1's real engine fixes this. The seam preview with the real engine works (scratch evidence, #29).
  - I1: wire `create_app(engine=get_engine(), bundle_loader=<B4 approved loader>)`.
  - I1: compare golden closure blocker lists as sets (#14).
  - I1: check PDF-001 on a `failed` extraction that has no page table (timeout or parse failure).
  - Single-process in-memory store.
  - The PDF size limit applies after receipt.
  - Timed-out extraction threads keep running.
  - Local HTTP cookie (`secure=False`).
  - No rate limiting.
  - The audit chain is not externally anchored.
  - The redaction eval set is self-written (1/19 not redacted, 1/19 over-redacted).
  - Third-party loggers are capped by name (a new one that logs content at WARNING would bypass).
  - Per-page-load workspaces are per user: two users never share decisions in the demonstrator (OPEN-3 consequence; state it in the brief).

Also executed outside the suite (#30): a real uvicorn server with markers gave 0 marker occurrences and no access log.

Hand-off to B4 (B4 starts from this commit):
  - You own `noteguard/api/routes/aggregate.py`. EDIT it; do not create a second route file.
  - Keep `Depends(require_aggregate_viewer)` (route layer).
  - Data: `request.app.state.store.aggregate_rows(staff)`. It re-checks the aggregate role at the store, audits `aggregate_read`, and returns rows of (rule_id, tier, state, created_at, owner_role, first_decision_at, disposition) with no ids and no text.
  - Yours: age buckets, counting, "<5" suppression and `ruleset_version`. The rows count per-user workspace copies of one synthetic case.
  - `tests/governance/test_governance.py::test_aggregate_no_content_or_ids` fails (501 != 200) until you replace the 501.
  - Egress for AI drafting: `from noteguard.redaction import get_redactor, prepare_egress`.
    - Call `prepare_egress(text, redactor=get_redactor(known_names))`, passing the encounter's staff names and patient label.
    - It returns a `QualifiedRedactedText` or raises ValueError (fail closed). The provider must still call `payload.assert_qualified()`.
  - Feedback: every decision records a `FeedbackEvent`; read them with `store.feedback_events(ctx, encounter_id)` (care-team scoped). There is no export route in the contract.
  - AI status: `GET /api/ai/status` lives in B2's `routes/core.py` and returns a constant "disabled". If your module must report a live status, ask @k; it's a small change on B2's path, or a CCR if the shape changes.
  - Ruleset approval: I1 passes your approved loader as `create_app(engine=get_engine(), bundle_loader=<your loader>)`. With the stub engine it is never called.
For B3:
  - Hide or disable "Reset case" when `WorkspaceInfo.encounter_ids` is empty (#32).
  - With the stub engine, bubbles, summary and glance are 501 after any decision (flags and closure still work).
  - The session cookie has Path=/api.
  - The workspace token lives only in X-Workspace-Token (JS memory).
  - The document route needs the cookie only.

Next step (the first thing the next session should do): after @k merges b2-api, run `make test` on main and
confirm the tally above (NI 12, REAL 1 = the known aggregate test). Then answer B4/I1 questions on the seams.

## Session B2.1 — 23 Sep 2026, ~01:40–02:00 SGT — context file r4
Branch / last commit SHA: b2-api / dd698aa (no B2 commits; branch == main)
Built this session: opening ritual only; no code. An orphaned, uncommitted B2 tree in the scratch area was
  preserved outside the repo and not used (decisions/B2.md #1).
Last test command run, verbatim, and its summary line:
  $ make test      → 27 failed, 72 passed, 1 skipped (tally: NI 27 · REAL 0 · errors 0)
  $ make test-api  → 13 failed, 1 passed, 86 deselected (tally: NI 13 · REAL 0)
Tests owned → status → evidence level: all 13 B2 tests → not implemented → executed (baseline)
Not built or cut, with reason: everything; the session ended at the tool limit before code.
Contract change requests raised: none.
Known gaps; where this lane fails first: n/a (nothing built).
Next step: re-clone, confirm the baseline, then skeleton + authz + store + core routes, is_stub=False.

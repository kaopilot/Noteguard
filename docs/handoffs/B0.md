# B0 handoffs (newest first)

## Session B0.1 — 22 Sep 2026, start not recorded–09:58 SGT — context file r4
(One session across two chat windows; the interim handoff text given in chat before the
continuation is superseded by this entry.)

Branch / last commit SHA: b0-contracts / see `git log -1` (this note is committed in [B0.9]; the
code it describes is at a014afa and earlier)

Built this session: tooling (B0.1); frozen contracts incl. spans, egress gate, EvidenceRef,
AddPdfSourceForm, GLANCE route (B0.2, B0.4, B0.7); ruleset v1 + registry v1 + draft approval
(B0.3, B0.8); ENC-A1/ENC-B1 fixtures (B0.4); hand-written goldens ENC-A1_1130, ENC-A1_1600,
ENC-A1_1600_rerun, ENC-B1_1600 + materializer + REVIEW_SHEET.md (B0.5); StubEngine, stub API on
exactly ROUTE_TEMPLATES, docs/openapi.json (B0.6); test skeletons for all 9 required and all
lesson tests with owner markers, B0 contract tests, Makefile (B0.7); PLAN, DECISIONS,
decisions/B0, brief part, CCR template, held-out ENC-A2 delivered outside the repo (B0.8).

Last test command run, verbatim, and its summary line:
  $ make test
  ============= 48 failed, 22 passed, 1 skipped, 1 warning in 2.09s ==============
  tally: not implemented (lane not landed yet): 48 · REAL failures: 0 · setup/teardown errors: 0
  Same result from a fresh clone of b0-contracts after `make setup` (exit 0).

Tests owned → status → evidence level:
  test_contract_seams (2) → pass → executed
  test_forbidden_phrase_lint (2) → pass → executed
  test_golden_fixtures (3) → pass → executed
  test_spans (2) → pass → executed
  test_contract_states_permissions (7) → pass → executed
  test_stub_api_contract (4) → pass → executed (stub-serves-goldens retires when B2 lands)
  test_open_count_integrity (1) → pass → executed against the STUB only; I1 must rerun on real API
  test_log_allowlist (1) → pass → executed
  test_e2e_smoke → fails "not implemented" → skeleton only (I1)
  test_heldout_golden → skipped (not installed) → verified it loads and fails "not implemented"
    with NOTEGUARD_HELDOUT_DIR pointing at the package
  Mutation spot-checks (executed, reverted): duplicate role list → seam test fails; "was not
  done" in a rule template → lint fails; glance counting only `open` Tier 1 → count integrity fails.

Not built or cut, with reason:
  - Generated TypeScript types: not produced by B0; B3 generates them from docs/openapi.json.
  - OWN-001 golden: not written; its evidence shape is an open question (decisions/B0.md #22).
  - Nothing from the B0 card was cut.

Contract change requests raised: none (pre-CP0; contract changes made directly as B0 owner and
listed in decisions/B0.md #2, #11–#14).

Known gaps; where this lane fails first:
  - Goldens and held-out goldens are NOT signed off yet (CP0 is @k's review).
  - APPROVAL_v1.md is draft, so once B4's check lands the runtime will refuse to run checks until
    @k approves a ruleset.
  - Statement spans mis-split abbreviations such as "e.g. ".
  - The stub API enforces no authorisation; never demo it as the product.
  - test_access_control ends with a fault-injection step B2 must implement.

Next step: @k reviews fixtures/expected/REVIEW_SHEET.md and the held-out REVIEW_SHEET, confirms
the OPEN defaults and decisions #6 (SpO2/RR not critical in v1) and #19 (approval stays draft),
decides #22 (OWN-001 evidence), signs the review sheet, then merges b0-contracts → main (CP0)
and starts B1–B4. Give B1 chats the repo but never noteguard_heldout_ENC-A2.zip.

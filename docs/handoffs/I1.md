# I1 handoffs (newest first)

## Session I1.3 — 24 Sep 2026, ~02:00 SGT (container clock) — context file r4
(Same chat as I1.1-I1.2.)
Branch / last commit SHA: i1-integration / see `git log -1` (tree verified at 496739f = B3.4 fast-forwarded).

Built this session: B3.4 checked and fast-forwarded (decisions #36); all suites rerun; fresh clone repeated (#37).

Last test command run, verbatim, and its summary line:
  $ make test
  ================== 136 passed, 1 skipped, 1 warning in 46.93s ==================
  tally: not implemented (lane not landed yet): 0 · REAL failures: 0 · setup/teardown errors: 0
  $ make test-ui → Test Files 9 passed (9); Tests 30 passed (30)
  $ cd frontend && npm run e2e -- --repeat-each=2 → 24 passed (1.0m)
  Fresh clone at 496739f: make setup exit 0; make test exit 0, 136 passed, 1 skipped; --verify consistent.

Tests owned → status → evidence level: unchanged from I1.2; all pass, executed.

Not built or cut, with reason: nothing from I1's card.

Contract change requests raised: none.

Known gaps; where this lane fails first:
  - The smoke is desktop only (B3's walk covers 375 px).
  - The fresh clone ran in the same container.
  - ENC-A2 is in history: never hand a main bundle to B1.
  - B5: the README must include the Playwright chromium install, and the brief must state as-of intake.

Next step (the first thing the next session should do): @k merges `i1-integration` into `main` (a fast-forward from 00cbe6b;
  it already contains B1.5, CCR-05, B4.4 and B3.4). B5 finalises with "136 passed, 1 skipped".

## Session I1.2 — 24 Sep 2026, ~01:00–01:30 SGT (container clock) — context file r4
(Same chat as I1.1.)
Branch / last commit SHA: i1-integration / see `git log -1` (tree verified at 9e6791e). ccr-05 merged at 4700efa.

Built this session:
  - B4.4 checked (decisions #32) and fast-forwarded.
  - APPROVAL_v1 re-signed on @k's written authorisation, recorded by I1 after the checks (#33).
  - CCR-05 landed (#34).
  - Fresh clone repeated (#35).

Last test command run, verbatim, and its summary line:
  $ make test                  (merged tree)
  ================== 136 passed, 1 skipped, 1 warning in 43.65s ==================
  tally: not implemented (lane not landed yet): 0 · REAL failures: 0 · setup/teardown errors: 0
  Fresh clone at 9e6791e: make setup exit 0; make test exit 0, 136 passed, 1 skipped (NI 0, REAL 0).
  $ uv run python -m noteguard.governance.evaluate --baseline v1 --verify → APPROVAL_v1: consistent
  $ make test-governance → 9 passed

Tests owned → status → evidence level: as in I1.1, all pass, executed. The golden API test now compares OWN-001's
  reason to the golden itself (no override).

Not built or cut, with reason: nothing from I1's card. Merging to main is @k's.

Contract change requests raised: none new. CCR-05 landed.

Known gaps; where this lane fails first:
  - B3's walk after the swap: 2 of 12 fail (B3's).
  - The smoke is desktop only.
  - The fresh clone ran in the same container.
  - ENC-A2 is in history: never hand a main bundle to B1.
  - B5: the README must include `cd frontend && npx playwright install chromium`, and the brief must state as-of intake (#28).

Next step (the first thing the next session should do):
  1. @k merges `i1-integration` into `main` (a fast-forward if main is still 00cbe6b). B1.5 is already in this branch
     as d9ff797: do not re-apply it.
  2. B5 finalises with "136 passed, 1 skipped".
  I1 is complete for CP2; reopen only for a failing required test before release.

## Session I1.1 — 24 Sep 2026, 00:07–~01:00 SGT (container clock) — context file r4
Branch / last commit SHA: i1-integration / see `git log -1` (code at 4beabb5). Base: main 00cbe6b.
  Side branch: ccr-05 at 1ecdecb (CCR-05 step 1; red by design until B4, decisions #29).

Built this session (one commit per step):
  - 1: held-out ENC-A2 installed, 48399cb.
  - 2: rule set frozen (@k).
  - 3: APPROVAL_v1 approved on @k's written sign-off, e417c3f.
  - 4: engine swap in app.py, aea9b72.
  - 5: tests/e2e/test_api_real_engine.py, b735650 + 4adb915.
  - 6: E2E smoke, 4beabb5.
  - 7: B3 walk after the swap run (#25).
  - 8: B5 does the fold (@k).
  - 9: fresh clone (#31).
  - B1.5 patch applied as B1's own commit d9ff797: @k, do NOT apply it again on main.
  - CCR-05 raised, approved, step 1 done on `ccr-05`.

Last test command run, verbatim, and its summary line:
  $ make test                  (i1-integration at 4beabb5)
  ================== 136 passed, 1 skipped, 1 warning in 41.78s ==================
  tally: not implemented (lane not landed yet): 0 · REAL failures: 0 · setup/teardown errors: 0
  Fresh clone: make setup exit 0; make test exit 0, 136 passed, 1 skipped (NI 0, REAL 0).
  $ uv run python -m noteguard.governance.evaluate --baseline v1 --verify → APPROVAL_v1: consistent
  $ make test-governance → 9 passed
  $ cd frontend && npm run e2e (B3's walk, after the swap) → 2 failed, 10 passed (before the swap: 12 passed)
  On ccr-05: make test 2 failed, 134 passed, 1 skipped (NI 1 = smoke not on that branch; REAL 1 = B4 verify test, by design).
  The one skip is the retired stub-only contract check.

Tests owned → status → evidence level (all executed):
  - test_heldout_golden x2 → pass.
  - test_api_real_engine.py x5 (goldens through the API, count integrity incl. decisions, grounding, CRIT-001 routing,
    PDF-001 on a failed extraction) → pass; mutations 5/5 caught (#20, #27).
  - test_e2e_smoke → pass; mutations 2/2 caught (#30).
  - test_open_count_integrity (B0's file) → pass on create_app() with the stub. The real-engine run is I1's test above.

Not built or cut, with reason:
  - CCR-05 steps 2-4 wait on B4 and @k.
  - The smoke covers mobile layout nowhere (desktop only); B3's walk covers 375 px.
  - Merging to main is @k's.

Contract change requests raised: CCR-05 (approved; landing in progress on `ccr-05`).

Known gaps; where this lane fails first:
  - B3's main-path walk fails at both sizes after the swap (2 of 12); expected by B3, cause not diagnosed by I1; for B3.
  - Finding #19 was withdrawn (I1's own test error, #26).
  - Intake is as-of (B2 #8), so a golden-day cutoff ignores new uploads; the brief should say so (#28).
  - B2's API suite runs on the stub via real_app(); the real-engine API evidence is I1's tests.
  - ENC-A2 is in branch history (#5).
  - The fresh clone ran in the same container (#31).

Next step (the first thing the next session should do): when B4 has committed CCR-05 step 2 on `ccr-05` and @k has
  re-signed in writing naming the new report hash:
  1. On `ccr-05`, set `status`, `approver_label` and `approved_on`.
  2. `--verify` must print consistent; `make test-governance` must give 9 passed.
  3. Merge `ccr-05` into `i1-integration`; `make test` must show REAL 0.
  4. Re-bundle, and tell @k and B5 the `make test` line (expected: 136 passed, 1 skipped).

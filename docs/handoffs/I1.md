# I1 handoffs (newest first)

## Session I1.1 — 24 Sep 2026, 00:07–(open) SGT (container clock) — context file r4
STATUS: PAUSED AT @k's APPROVAL GATE (step 3). This entry is interim; it is updated when the session resumes or ends.

Branch / last commit SHA: i1-integration / see `git log -1` (this note is its own [I1.1] commit). Base: main 00cbe6b (after B4.3).
  Code/fixture commit: 48399cb (held-out). Decisions commit: 283a53b.

Built this session:
  - Step 1: held-out ENC-A2 installed in `fixtures/heldout/` (custody checks in decisions/I1.md #2-5).
  - Step 3, read-only part only: `--verify` and a fresh evaluation run into /tmp (decisions #7). APPROVAL_v1.md NOT edited.
  - Environment checks, no repo change: Playwright's chromium is preinstalled here (#9); B3's walk before the swap (#10).
  - NOT done: steps 2 (freeze), 3 (approval fields), 4 (engine swap), 5 (API-level tests), 6 (E2E smoke), 7 (walk after the swap), 8 (fold), 9 (fresh clone).

Last test command run, verbatim, and its summary line:
  $ make test                      (baseline, main 00cbe6b)
  ============= 1 failed, 127 passed, 2 skipped, 1 warning in 22.37s =============
  tally: not implemented (lane not landed yet): 1 · REAL failures: 0 · setup/teardown errors: 0
  $ uv run pytest tests/engine/test_heldout_golden.py -v
  ========================= 2 passed, 1 warning in 0.34s =========================   (ENC-A2_1200, ENC-A2_1730)
  $ make test                      (after the held-out commit 48399cb)
  ============= 1 failed, 129 passed, 1 skipped, 1 warning in 21.59s =============
  tally: not implemented (lane not landed yet): 1 · REAL failures: 0 · setup/teardown errors: 0
  $ uv run python -m noteguard.governance.evaluate --baseline v1 --verify
  APPROVAL_v1: consistent
  $ uv run python -m noteguard.governance.evaluate --baseline v1 --candidate v1 --out-dir /tmp/eval
  EVAL_v1_vs_v1: gate passed; report sha256 81cc969e384be097761f2e82b9b2bd54cae97cb82ff305a02db43d5a6f18d8cc
  $ cd frontend && npm run e2e     (B3's walk, BEFORE the swap)
  12 passed (34.3s)

Tests owned → status → evidence level:
  test_heldout_golden x2 → pass → executed (engine level; full exact-set comparison incl. closure blockers as a set).
  test_open_count_integrity → pass → executed, but against B2's real API with the STUB engine (the real-engine run is step 5).
  test_e2e_smoke → fails "not implemented" → not produced (step 6 waits for steps 3-4).
  I1 API-level runs (goldens, count integrity, grounding, CRIT-001 routing, PDF-001 on a failed extraction) → not produced (step 5 waits for step 4).

Not built or cut, with reason: steps 2-9 wait on @k (below). ENC-A2 is not added to the evaluation corpus (@k's instruction; decisions #6).

Contract change requests raised: none.

Known gaps; where this lane fails first:
  - Check runs through `noteguard.api.app:app` still use the STUB engine; `main` still has a stub in the request path (CP2 not met).
  - ENC-A2 is now in the branch history: any bundle of it carries ENC-A2 (decisions #5). It was also partially seen by B1 (B1.4).
  - APPROVAL_v1.md prose says DRAFT; after approval it contradicts the JSON unless B4 or @k updates it (decisions #8).

Waiting on @k (one reply is enough):
  1. Step 2: confirm no further edits to `rulesets/v1.json` or `rulesets/registry_v1.json`.
  2. Step 3: explicit written approval of v1 and the sign-off date for `approved_on`.
  3. Who updates the APPROVAL_v1.md prose (B4, or I1 with your go-ahead).
  4. Step 8: who folds `docs/decisions/B*.md` into `docs/DECISIONS.md` (B5 owns the final file).
  If approval cannot happen before the 08:00 freeze: your call on the fallback (swap without `bundle_loader`; the brief then says the gate is built and tested but not switched on).

Next step (the first thing the next session should do): read @k's answers. With written approval, set the three JSON fields,
  rerun `--verify` (must print consistent) and `make test-governance` (9 passed), commit; then step 4.

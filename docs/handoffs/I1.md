# I1 handoffs (newest first)

## Session I1.1 — 24 Sep 2026, 00:07–(open) SGT (container clock) — context file r4
STATUS: PAUSED AT @k's APPROVAL GATE (step 3). Interim entry; updated when the session resumes or ends.

Branch / last commit SHA: i1-integration / see `git log -1` (this note is its own [I1.1] commit). Base: main 00cbe6b (after B4.3).
  Held-out commit: 48399cb. Docs: decisions/I1.md #1-16.

Built this session:
  - Step 1: held-out ENC-A2 installed and committed (custody: decisions #2-5).
  - Step 3, read-only part: `--verify` consistent; fresh report byte-identical to B4's (#7). APPROVAL_v1.md NOT edited.
  - Findings for @k: the fallback is not implementable in I1's paths (#12); OWN-001 reason drift between golden and ruleset (#13).
  - Step 5 DRAFTS, uncommitted (red until approval), plus a scratch preview (#14). Copies: /mnt/user-data/outputs/i1_drafts/.
  - NOT done: steps 2, 3 (approval fields), 4, 5 (committed and mutation-checked), 6, 7, 8, 9.

Last test command run, verbatim, and its summary line:
  $ make test                      (baseline, main 00cbe6b)
  ============= 1 failed, 127 passed, 2 skipped, 1 warning in 22.37s =============
  tally: not implemented (lane not landed yet): 1 · REAL failures: 0 · setup/teardown errors: 0
  $ uv run pytest tests/engine/test_heldout_golden.py -v
  ========================= 2 passed, 1 warning in 0.34s =========================   (ENC-A2_1200, ENC-A2_1730)
  $ make test                      (after 48399cb; the uncommitted drafts did not exist yet)
  ============= 1 failed, 129 passed, 1 skipped, 1 warning in 21.59s =============
  tally: not implemented (lane not landed yet): 1 · REAL failures: 0 · setup/teardown errors: 0
  $ uv run python -m noteguard.governance.evaluate --baseline v1 --verify
  APPROVAL_v1: consistent
  $ uv run python -m noteguard.governance.evaluate --baseline v1 --candidate v1 --out-dir /tmp/eval
  EVAL_v1_vs_v1: gate passed; report sha256 81cc969e384be097761f2e82b9b2bd54cae97cb82ff305a02db43d5a6f18d8cc
  $ cd frontend && npm run e2e     (B3's walk, BEFORE the swap)
  12 passed (34.3s)
  Scratch only (not evidence for any claim): the drafts ungated, as described in decisions #14.

Tests owned → status → evidence level:
  test_heldout_golden x2 → pass → executed (engine level).
  test_open_count_integrity (B0 file) → pass → executed, but with the STUB engine behind the real API.
  test_e2e_smoke → fails "not implemented" → not produced (step 6).
  tests/e2e/test_api_real_engine.py (5 drafts) → NOT RUN as tests (uncommitted; gated they give 503 before approval) → not produced.

Not built or cut, with reason: steps 2-9 wait on @k. ENC-A2 is not in the evaluation corpus (#6).

Contract change requests raised: none yet. #13 needs one if @k picks option (a).

Known gaps; where this lane fails first:
  - `noteguard.api.app:app` still uses the STUB engine; CP2 is not met.
  - OWN-001 reason drift (#13): the step-5 golden test cannot go green until @k rules.
  - The fallback gives 501, not a working demo (#12).
  - ENC-A2 is in branch history (#5). APPROVAL_v1.md prose says DRAFT (#8).

Waiting on @k:
  1. Step 2: rule set freeze, which depends on how #13 is settled.
  2. Step 3: explicit written approval and the sign-off date.
  3. The APPROVAL prose (#8).
  4. The decisions fold (step 8).
  5. #13: option (a) CCR, or (b).

Next step (the first thing the next session should do): read @k's answers. Then fix the two draft bugs (#14: resolve body; PDF
  source-text assertion) in scratch mode. After written approval: set the three fields, `--verify`, `make test-governance`, commit;
  step 4; commit the step-5 tests once green gated, with mutations applied; then steps 6-9.

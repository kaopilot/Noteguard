# I1 handoffs (newest first)

## Session I1.1 — 24 Sep 2026, 00:07–(open) SGT (container clock) — context file r4
STATUS: steps 1-5 done and committed; step 6 (E2E smoke) IN PROGRESS, uncommitted; step 7 run (decisions #25); step 9 not run.
Branch / last commit SHA: i1-integration / see `git log -1`. Base: main 00cbe6b.

Built this session:
  - 1: held-out ENC-A2, 48399cb.
  - 2: frozen (@k).
  - 3: v1 approved, e417c3f.
  - 4: engine swap, aea9b72.
  - 5: tests/e2e/test_api_real_engine.py, b735650.
  - CCR-05 written (approved, not landed).
  - 8: B5 folds (@k).
  - Findings: #12 (fallback gives 501), #13 → CCR-05, #19 (no PDF-001 on a failed extraction).
  - Correction to @k: #18.

Last test command run, verbatim, and its summary line:
  $ make test    (at b735650; the committed smoke is still the skeleton)
  ======= 1 failed, 133 passed, 1 skipped, 1 xfailed, 1 warning in 25.18s ========
  tally: not implemented (lane not landed yet): 1 · REAL failures: 0 · setup/teardown errors: 0
  $ uv run python -m noteguard.governance.evaluate --baseline v1 --verify   → APPROVAL_v1: consistent (approved record)
  $ make test-governance → 9 passed
  $ uv run pytest tests/e2e/test_e2e_smoke.py -q   (UNCOMMITTED draft) → 1 failed (#24)
  $ cd frontend && npm run e2e   (after the swap) → decisions #25

Tests owned → status → evidence level:
  test_heldout_golden x2 → pass → executed.
  test_golden_scenarios_through_real_api, test_open_count_integrity_real_api, test_grounding_through_real_api,
    test_critical_observation_routing_real_api → pass → executed; mutations 4/4 caught (#20).
  test_pdf001_on_failed_extraction_without_page_table → xfail(strict) → executed; it is FINDING #19 (B1 via @k).
  test_open_count_integrity (B0's) → pass → executed on create_app() (stub); the real-engine run is I1's test above.
  test_e2e_smoke → committed tree: not implemented; draft: fails at step 4 → not produced.

Not built or cut, with reason:
  - Step 6 not green yet.
  - Step 9 (fresh clone) not run.
  - CCR-05 not landed (needs B4's report refresh + @k's re-sign, #18).

Contract change requests raised: CCR-05 (approved, not landed).

Known gaps; where this lane fails first:
  - CP2: real engine behind the gated API = DONE (committed); goldens through the API = 5/5 (OWN-001 reason per CCR-05);
    E2E smoke = NOT GREEN; `main` not merged (@k merges i1-integration).
  - #19 PDF-001 on a failed extraction.
  - B3's walk after the swap (#25).
  - ENC-A2 is in branch history (#5).
  - The smoke covers desktop only.

Next step (the first thing the next session should do): copy outputs/i1_drafts/{smoke.spec.ts,playwright.config.ts,test_e2e_smoke.py}
  into tests/e2e/ if they are not in the working tree. Find the navigation control after the Closure view (B3's app; B3's
  walk clicks 'Review' only after 'Summary'), run the smoke to green, apply its mutation, commit. Then step 9 (fresh clone),
  the final handoff, and tell @k and B5 the `make test` summary line.

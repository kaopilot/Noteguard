# B4 handoffs (newest first)

## Session B4.4 — 24 Sep 2026, ~01:25–01:45 SGT (container clock) — context file r4
(Same chat. Task from @k: CCR-05 step 2 on I1's branch `ccr-05`.)

Branch / last commit SHA: ccr-05 / see `git log -1` (this note is in the last [B4.4] commit).
Base: 1ecdecb ([I1.1] CCR-05 step 1). Work commit: 45ee87d.

Built this session:
- Report regenerated: `fixtures/labelled_eval/reports/EVAL_v1_vs_v1.{json,md}`, sha256 `ba5f0711db400073c73caf1e61972a3f466b86672dcb570ce7be1a267225c725`.
  - Equal to I1's independent value.
  - Field-level diff: only `corpus.sha256` changed. Gate passed; Tier 1 recall 17/17.
- `rulesets/APPROVAL_v1.md`: `evaluation_report_sha256` set to the new hash, `status` set to "draft", and the prose status line matches.
  - `approver_label` and `approved_on` are untouched; I1 sets them at step 4.
  - Not approved on @k's behalf.
- `--verify` → `APPROVAL_v1: consistent`. The loader refuses with `not_approved` only.

Last test command run, verbatim, and its summary line:
  $ make test
  ============= 6 failed, 130 passed, 1 skipped, 1 warning in 20.33s =============
  tally: not implemented (lane not landed yet): 1 · REAL failures: 5 · setup/teardown errors: 0
  (The "ERROR noteguard:logs.py" lines in the output are B2's captured structured log of the refused runs
  — route, 503, ruleset_unapproved, no content — not test errors.)
  Every failing test id and its cause (--tb=line):
    tests/e2e/test_api_real_engine.py::test_golden_scenarios_through_real_api       503 ruleset_unapproved (line 74)
    tests/e2e/test_api_real_engine.py::test_open_count_integrity_real_api           503 ruleset_unapproved (line 74)
    tests/e2e/test_api_real_engine.py::test_grounding_through_real_api              503 ruleset_unapproved (line 74)
    tests/e2e/test_api_real_engine.py::test_critical_observation_routing_real_api   503 (tests/api/helpers.py:48)
    tests/e2e/test_api_real_engine.py::test_pdf001_on_failed_extraction_without_page_table  503 ruleset_unapproved (line 201)
    tests/e2e/test_e2e_smoke.py::test_e2e_smoke   NotImplementedError (I1's smoke is not committed on ccr-05; NOT a 503)
  $ make test-governance → 9 passed, 128 deselected (not implemented 0 · REAL 0)
  Baseline at session start (ccr-05 as delivered, 1ecdecb):
    2 failed, 134 passed, 1 skipped (NI 1 smoke · REAL 1 = test_approval_verify_and_offline_clis), matching CCR-05 step 1.
    That REAL failure now passes.

Tests owned → status → evidence: all 9 B4 tests pass (executed). No test was edited this session.

Not built or cut: nothing in scope was cut. B3's Playwright walk was not run; it is not part of `make test`, and with draft status it would show "Checks are paused".

Contract change requests: CCR-05 step 2 done; steps 3–4 pending (below). No other CCR.

WHAT NEEDS TO BE DONE NEXT (CCR-05 landing steps 3–4):
1. @k: re-sign in writing, naming report sha256 `ba5f0711db400073c73caf1e61972a3f466b86672dcb570ce7be1a267225c725`.
2. I1, in the JSON block of `rulesets/APPROVAL_v1.md`:
   - set `"status": "approved"`, and `approver_label` / `approved_on` per the re-sign;
   - update the prose status line;
   - then check:
     - `--verify` prints `APPROVAL_v1: consistent`;
     - `make test-governance` gives 9 passed;
     - `make test` shows REAL 0. EXPECTED, NOT RUN: the five API tests above pass, and the only failure left is I1's smoke (not implemented) until it is committed.
3. I1: merge `ccr-05` into `i1-integration`.
4. B5: the brief states the approval-evidence design (decisions #54). The final approval state comes from I1's step 4.

Next step (the first thing the next B4 session should do): nothing is pending in B4 once step 4 is green. If `--verify` ever prints anything other than consistent, regenerate the report and refresh the hash, as in this session.

## Session B4.3 — 23 Sep 2026, evening SGT (container clock) — context file r4
(Same chat. New main from @k: B4 merged; B2.3/B2.4, B3.2/B3.3 merged; CCR-03 and CCR-04 approved; CCR-04 part 1 landed by @k.)

Branch / last commit SHA: b4-governance / see `git log -1` (this note is in the last [B4.3] commit). Base: main 139a600.

Built this session:
- CCR-04 part 2: `AggregateCell.response_time_bucket` ("<1h" | "1-4h" | ">4h" | "none_yet").
  - Filled from B2's `first_decision_at` in `governance/aggregate.py`.
  - It is part of the cell key, so "<5" suppression applies to the finer cells.
- `test_aggregate_no_content_or_ids` extended:
  - "none_yet" before any decision;
  - a real accept through the API moves one copy to a timed bucket (DOSE-001: open "5", accepted "<5");
  - boundaries checked as direct inputs;
  - split cells both suppressed.
- Stale route comment fixed.
- Separable INTEGRATOR-OWNED `make types` commit (4bf22d5), and the CCR-04 status line updated.

Last test command run, verbatim, and its summary line:
  $ make test
  ============= 1 failed, 127 passed, 2 skipped, 1 warning =============
  tally: not implemented 1 (I1's test_e2e_smoke) · REAL failures 0 · errors 0
  $ make test-governance → 9 passed (not implemented 0 · REAL 0)
  $ cd frontend && npm run typecheck && npm test && npm run build
    → tsc clean; Test Files 9 passed (9), Tests 29 passed (29); built
  $ uv run python -m noteguard.governance.evaluate --baseline v1 --verify → APPROVAL_v1: consistent
  Baseline at session start (139a600): make test 1 failed, 127 passed, 2 skipped (NI 1 · REAL 0).

Tests owned → status → evidence: all 9 pass (executed). M15 and M16 applied and caught (16/16 across B4).

Not built or cut: unchanged from B4.2 (monitoring, blind sample, shadow ranking, AI route); complementary suppression.

Contract change requests: CCR-04 part 2 landed here; CCR-03 has no B4 impact.

WHAT NEEDS TO BE DONE NEXT (in order; owner):
1. @k: merge b4-governance (B4.3).
   - Expect `make test`: 1 failed (I1 e2e, not implemented), 127 passed, 2 skipped; REAL 0.
   - To regenerate the artefacts yourself: drop 4bf22d5 and run `make types`. The output should be identical.
2. B3:
   - Render the response-time labels verbatim: `humanize()` turns "1-4h" into "1 4h". Humanize only "none_yet", or use a label map.
   - Optionally replace the local AggregateCell/AggregateView types with the generated ones.
   - Rerun the aggregate walk to see the new column.
3. I1 at CP2: unchanged; follow steps 3a–3g in the B4.2 entry. `--verify` is consistent on today's main.
4. Optional (@k): add held-out ENC-A2 to the evaluation corpus before approval.
5. B5:
   - The trace row for owner response time is now executed (CCR-04).
   - Fold decisions #41–49.

Next step (the first thing the next session should do): after the merge, run `make test` on main (expect REAL 0), then support I1 through CP2.

## Session B4.2 — 23 Sep 2026, ~18:05–18:15 SGT (container clock) — context file r4
(Same chat as B4.1: a double-check pass requested by @k.)

Branch / last commit SHA: b4-governance / see `git log -1` (this note is in the last [B4.2] commit). Code at 2ebba7c.

Built this session:
- `evaluate.verify_approval` + `--verify`. An approved record must match its files, the committed report, and a fresh rerun that passes the gate.
- New test `test_approval_verify_and_offline_clis`. It isolates each verify problem, checks the real record whenever it is approved, and runs both CLIs on files.
- L11 parameter-effect assertions for `noisy_dismissal_rate`, `reading_floor_ms`, the aggregate `threshold` and the loader `version`.
- Brief part: added the exposure-bias statement and the Section 11 items not built; trimmed to about half a page.

Checked, no change needed:
- The branch touches only B4-owned paths (+ CCR-04).
- Both CLIs execute on files.
- The frontend suite is 24/24.
- `evaluate --verify` on the real draft reports "consistent".

Last test command run, verbatim, and its summary line:
  $ make test-governance
  ================= 9 passed, 118 deselected, 1 warning in 2.26s =================
  tally: not implemented 0 · REAL failures 0
  $ make test
  ============= 2 failed, 123 passed, 2 skipped, 1 warning in 19.45s =============
  tally: not implemented 1 (I1's e2e) · REAL failures 1 (B2-owned test_aggregate_route_is_b4_placeholder_behind_b2_authz; see B4.1)
  $ make test-ui → Test Files 7 passed (7); Tests 24 passed (24)

Tests owned → status → evidence: the 8 from B4.1 → pass; test_approval_verify_and_offline_clis → pass. All executed; mutation M14 applied and caught.

Not built or cut, with reason (Section 11, declared in the brief part):
- Dismissal-spike monitoring against the baseline (step 6).
- A blind sample of non-flagged sources (step 2, missed concerns).
- Learned or shadow ranking; ranking stays deterministic.
- No route calls the AI drafter.
- Aggregate page (B3).

Contract change requests raised: none new (CCR-04 proposed in B4.1).

WHAT NEEDS TO BE DONE NEXT (in order; owner):
1. @k: merge b4-governance. main then shows one REAL failure until step 2 lands (mirror of ruling #33).
2. B2: `tests/api/test_api_behaviour.py` line 430 → `assert r.status_code == 200 and "cells" in r.json()`.
   Optional: `ai_status()` returns `app.state.ai_drafter.status_view()` (identical output today).
3. I1 at CP2. The order matters, or the demo and E2E show 503 `ruleset_unapproved`:
   a. Freeze the final ruleset and registry.
   b. `uv run python -m noteguard.governance.evaluate --baseline v1 --candidate v1`: the gate must pass. Commit the report.
   c. If any file changed, refresh the hashes and the report hash in `rulesets/APPROVAL_v1.md`.
   d. @k sets status `approved`, label "Synthetic demo approver", and the date.
   e. `uv run python -m noteguard.governance.evaluate --baseline v1 --verify` must print "consistent".
   f. Wire `create_app(engine=get_engine(), bundle_loader=noteguard.governance.approval.api_bundle_loader())`.
   g. Run E2E and `make test`. `test_unapproved_ruleset_refused` and the verify test then check the approved record automatically; no test edit is needed.
4. B3: map error code `ruleset_unapproved` to plain copy, e.g. "Checks are paused: the rule set is awaiting clinical governance approval." Today the check run shows the raw code. A 503 with that code on bubbles or summary should not be labelled an outage. The aggregate page is optional per the cut order and needs CCR-04's types.
5. @k: rule on CCR-04 (aggregate OpenAPI + response-time bucket); CCR-03 is also still proposed.
6. Optional, stronger evidence (@k to decide; B4 can do it in about 30 min): add held-out ENC-A2 as a third gated part of the evaluation corpus once I1 installs it. It is the most independent labelled case we have. Doing so changes the report hash, so it must happen before step 3d.
7. B5: fold `docs/brief_parts/B4.md` and `docs/decisions/B4.md`.
   REQUIREMENTS_TRACE rows for B4, executed: L13 (both tests), protected floor, Tier 1 recall gate, aggregate no content/ids, AI disabled/timeout/validator, approval verify.
   Not produced (reason): monitoring, blind sample, shadow ranking, AI route.
   The brief must state exposure bias (Section 11 step 7).

Known gaps; where this lane fails first: unchanged from B4.1, plus the frontend copy for `ruleset_unapproved` (B3's path).

Next step (the first thing the next session should do): after the merge, run `make test` on main and confirm "REAL 1 = B2's placeholder test" (or 0 once B2 has fixed line 430). Then support I1 through step 3.

## Session B4.1 — 23 Sep 2026, ~17:30–18:05 SGT (container clock) — context file r4
(One session across three chat turns; the first ended at the opening ritual.)

Branch / last commit SHA: b4-governance / see `git log -1` (this note is in the last [B4.1] commit).
Base: main 850ac2c. Code complete at 956bb7d.

Built this session:
- `noteguard/governance/approval.py`: the approval check at ruleset load.
  - Refuses unless the record is approved, cites an evaluation report, and its hashes and versions match. Every reason is listed.
  - Re-verifies on every call; never caches.
  - `api_bundle_loader()` gives 503 `ruleset_unapproved` through B2's handler.
- `propose.py`: offline proposals.
  - Protected floor read from the baseline; suppressing-cue synonyms refused; new version required.
  - Per-rule metrics with denominators; dispositions counted per flag.
- `labelled.py` + `fixtures/labelled_eval/cases.json`: 18 gated cases + 1 known limit, labels committed before any engine run.
- `evaluate.py`: runs the real engine.
  - Per-rule TP/FP/FN with precision and recall as n/d.
  - Static floor check; Tier 1 recall gate; deterministic report.
  - Report: `fixtures/labelled_eval/reports/EVAL_v1_vs_v1.{json,md}` (gate PASSED, Tier 1 recall 17/17, sha256 81cc969e…).
- `aggregate.py` + `noteguard/api/routes/aggregate.py`: the real aggregate view. B2's route guard and store re-check are kept; age buckets; "<5".
- `ai_drafting.py`: off by default.
  - Verified cluster; egress through B2's `prepare_egress`; hard timeout; circuit breaker.
  - Validator; rule-text fallback; status reports observed outcomes only.
- `rulesets/APPROVAL_v1.md`: still DRAFT, now with current file hashes and the report hash. Refused only as `not_approved`.
- `docs/contract_changes/CCR-04.md` (proposed).

Last test command run, verbatim, and its summary line:
  $ make test-governance
  ================= 8 passed, 118 deselected, 1 warning in 1.76s =================
  tally: not implemented (lane not landed yet): 0 · REAL failures: 0 · setup/teardown errors: 0
  $ make test
  ============= 2 failed, 122 passed, 2 skipped, 1 warning in 22.30s =============
  tally: not implemented (lane not landed yet): 1 · REAL failures: 1 · setup/teardown errors: 0
    not implemented: tests/e2e/test_e2e_smoke.py::test_e2e_smoke (I1's)
    REAL: tests/api/test_api_behaviour.py::test_aggregate_route_is_b4_placeholder_behind_b2_authz
          (B2-owned; asserts the 501 placeholder B4 replaced; NOT edited; see "Needs" below)
  Baseline at session start (850ac2c):
    make test: 8 failed, 116 passed, 2 skipped (NI 7 · REAL 1 = the aggregate 501, known)
    make test-governance: 7 failed, 1 passed (NI 6 · REAL 1)

Tests owned → status → evidence level (all executed; mutations M1–M13 applied, 13/13 caught, decisions #34):
  test_no_runtime_rule_mutation (L13) → pass
  test_unapproved_ruleset_refused (L13) → pass
  test_protected_floor_refused → pass
  test_eval_report_tier1_recall_gate → pass
  test_aggregate_no_content_or_ids → pass (strengthened: the B0 body passed on an empty view)
  test_ai_disabled_by_default → pass (through B2's constant route; B4's Drafter default gives the same view, asserted in test_ai_timeout_falls_back)
  test_ai_timeout_falls_back → pass
  test_ai_output_validator_rejects_new_entity → pass

Not built or cut, with reason:
- No route calls the AI drafter: the contract has only /api/ai/status. The drafter is a gated library; the product ships disabled (OPEN-5).
- Aggregate page (B3) not built. Cut order allows endpoint-only; the generated types lack AggregateView (CCR-04).
- Owner response time in the aggregate view needs CCR-04.
- No complementary small-cell suppression.
- The evaluation was not rerun on a FINAL ruleset. That is I1's CP2 step.

Contract change requests raised:
- CCR-04 (proposed): declare AggregateView in OpenAPI, and add `AggregateCell.response_time_bucket`.
- No contract file edited. Integrator-owned files untouched: `docs/openapi.json` stays in sync because the route uses `response_model=None`.

Needs from other lanes:
- B2 / integrator: update `test_aggregate_route_is_b4_placeholder_behind_b2_authz` line 430 to expect 200, e.g. `assert r.status_code == 200 and "cells" in r.json()`. Until then `main` shows this one REAL failure after the B4 merge (mirror of @k ruling #33).
- B2 (optional): `ai_status()` → `request.app.state.ai_drafter.status_view()` with a default `Drafter()`. Same output today; it stays truthful if AI is ever enabled.
- I1: `create_app(engine=get_engine(), bundle_loader=noteguard.governance.approval.api_bundle_loader())`. Check runs return 503 `ruleset_unapproved` until @k approves v1.
- I1 at CP2:
  1. Run `uv run python -m noteguard.governance.evaluate --baseline v1 --candidate v1`.
  2. If the gate passes and the hashes in APPROVAL_v1.md still match: @k sets status `approved`, label "Synthetic demo approver", and the date. If any file changed, refresh the hashes and the report hash first.
  3. Afterwards, `test_unapproved_ruleset_refused` asserts the real record now loads. Its oracle is status-aware, so no test edit is needed.
- B1 / I1 (observation only): on a rerun with prior flags, the engine keeps a flag's earlier tier after the ruleset re-tiers the rule (decisions #33).

Known gaps; where this lane fails first:
- The evaluation corpus uses registry vocabulary (cases written after reading the registry). It measures generalisation across roster and wording, not paraphrase. Golden cases are not independent.
- The AI validator knows only registry entities. A drug outside the registry in model output is not caught.
- Certainty is two-level. Polarity is only "a negated sentence must cite a negated quote".
- A timed-out provider thread keeps running.
- The aggregate `ruleset_version` is the pinned version, not per flag.
- Counts are per-user workspace copies of one synthetic case.
- Process: commit 76918bc was red on this branch (seam test); fixed in 5ccefba.

Next step (the first thing the next session should do): after @k merges b4-governance, run `make test` on main. Expect REAL 1: B2's placeholder test, until B2 updates line 430. Then answer I1's CP2 questions: evaluation rerun, APPROVAL_v1.md, CCR-04.

# B5 handoffs (newest first)

## Session B5.2 — 24 Sep 2026, start/end times not recorded — context file r4
Branch / last commit SHA: b5-docs / see `git log -1`. `b5-docs` was fast-forwarded to I1.6's `handle-rename` (a9b9f45), then B5.2 commits 4b20332 (B5 docs) and 5800cd2 (separable handle fix) were added, plus this note.

Built this session:
  - Checked I1.6's repo-wide "@k" → "@kaopilot" rename (decisions #15). Everything outside `docs/` is a comment, docstring or prose line. No pinned input moved; the held-out manifest equals its pin (8/8); `--verify` is consistent; `make goldens` is byte-identical. I1 #41's claims reproduce.
  - Fixed the B5 sentences the blanket replace made circular or false: PLAN, brief part, the DECISIONS handle section; quoted "@k" restored in decisions #3 and the B5.1 handoff (decisions #16).
  - Renamed the two hyphenated misses (`@k-signed`) in a separable commit (decisions #17).
  - Updated the trace's evidence runs R1, R2, R4 and R5 and the brief header to the B5.2 results.

Last test command run, verbatim, and its summary line:
  $ make test                               (a9b9f45, I1.6 tip, as delivered: B5.2 baseline)
  ================== 136 passed, 1 skipped, 1 warning in 51.48s ==================
  tally: not implemented (lane not landed yet): 0 · REAL failures: 0 · setup/teardown errors: 0
  $ uv run pytest -q -rA --junitxml=<tmp>   (5800cd2) → 136 passed, 1 skipped, 1 warning in 41.72s; 68 cited tests all exist and pass (except the deliberate skip)
  FRESH CLONE (bundle of b5-docs at 5800cd2 → `git clone -b b5-docs`, README steps verbatim):
    $ make setup → exit 0
    $ (cd frontend && npx playwright install chromium) → exit 0
    $ make test → exit 0
    ================== 136 passed, 1 skipped, 1 warning in 44.20s ==================
    tally: not implemented 0 · REAL failures 0 · setup/teardown errors 0
    $ uv run python -m noteguard.governance.evaluate --baseline v1 --verify → APPROVAL_v1: consistent
    Clone clean afterwards. Same-container caveat as B5.1.
  The final commit (this note, the trace, brief and brief-part text) is docs-only on top of 5800cd2.

Tests owned → status → evidence level: B5 owns no tests; none written.

Not built or cut, with reason: nothing new. The B3 browser walk was not re-run: `frontend/src` is unchanged since B5.1's run (only a comment in `frontend/tests/feedback.test.tsx` changed, and `make test`'s Vitest wrapper covers that file).

Contract change requests raised: none.

Known gaps; where this lane fails first: as B5.1, minus the handle item. "@k" now remains only in hash-pinned places and in sentences quoting the old token (decisions #18).

Next step (the first thing the next session should do): @kaopilot fast-forwards `main` to `b5-docs` (it contains I1.6's `handle-rename`, so there is no need to merge that separately) and runs `make test` (expect 136 passed, 1 skipped). Commit 5800cd2 may be dropped if B1 or the integrator object to B5 touching their two lines.

## Session B5.1 — 24 Sep 2026, start/end times not recorded (one chat, two turns) — context file r4
Branch / last commit SHA: b5-docs / see `git log -1` (this note is in the last [B5.1] commit; the docs it describes are at e470f3f). Base: main eff8608 (after I1 / CP2).

Built this session:
  - `docs/REQUIREMENTS_TRACE.md`: the full ledger.
    - Builder Brief §1–8: 49 rows (40 hold, 9 partial).
    - Required proofs P1–P9 = appendix micro-tests 1–9: 8 hold, P5 partial.
    - Appendix extras: 10 rows (7 hold, 1 partial, 2 not built).
    - Lessons L1–L20 and the Section 4 checklist.
    - A not-built list.
    - Evidence runs R1–R5 on top.
  - `docs/TECHNICAL_BRIEF.md`: all 12 Section 15 headings, edited from the five brief parts, with stale lines corrected (decisions #9).
  - `README.md`: setup, test and run commands from the Makefile, the Playwright install, how to run the UI, the synthetic roster, honest status and the posture table.
  - Final `docs/PLAN.md` (schedule actually followed, cuts actually made) and the `docs/DECISIONS.md` fold (one line per consequential lane decision).
  - `docs/decisions/B5.md` #1–14, `docs/brief_parts/B5.md`.
  - Owner handle @kaopilot in all of the above. Lane records, goldens, the held-out package and APPROVAL_v1 are unchanged (decisions #3).

Last test command run, verbatim, and its summary line:
  $ make test                      (b5-docs, docs staged, before commit e470f3f)
  ================== 136 passed, 1 skipped, 1 warning in 44.69s ==================
  tally: not implemented (lane not landed yet): 0 · REAL failures: 0 · setup/teardown errors: 0
  Baseline at session start (main eff8608): make setup exit 0; make test 136 passed, 1 skipped, 1 warning in 45.67s (NI 0, REAL 0, errors 0).
  $ uv run pytest -q -rA --junitxml=<tmp>  → 136 passed, 1 skipped, 1 warning in 43.23s (per-test results for the trace)
  $ uv run python -m noteguard.governance.evaluate --baseline v1 --verify → APPROVAL_v1: consistent
  $ cd frontend && npm run e2e → 12 passed (40.8s), exit 0 (375 px and 1440 px, real engine)
  FRESH-CLONE RELEASE CHECK (exit criterion). Setup: bundle of b5-docs at e470f3f → `git clone -b b5-docs` into /tmp/fresh, then the README steps verbatim:
    $ make setup                                        → exit 0
    $ (cd frontend && npx playwright install chromium)  → exit 0
    $ make test                                         → exit 0
    ============================= Noteguard lane tally =============================
    not implemented (lane not landed yet): 0
    REAL failures: 0
    setup/teardown errors: 0
    ================== 136 passed, 1 skipped, 1 warning in 45.31s ==================
    $ uv run python -m noteguard.governance.evaluate --baseline v1 --verify → APPROVAL_v1: consistent
    $ uv run pytest -q tests/engine/test_heldout_golden.py tests/e2e/test_e2e_smoke.py → 3 passed (held-out ran, not skipped)
    Clone clean afterwards (`git status --short` empty).
    Caveat: same container (Chromium preinstalled at /opt/pw-browsers, package caches warm), not a clean machine.

Tests owned → status → evidence level: B5 owns no tests and wrote none (no mutation spot-check due; decisions #8). Trace integrity was checked by a scratch script, not committed: 68 cited test names all exist and passed in R2, except the deliberate skip `test_stub_serves_goldens` (decisions #7).

Not built or cut, with reason:
  - B5 did not draft from CP0 as the schedule planned; it started after CP2 (PLAN records this).
  - No test for "a note author does not automatically gain access" (trace P5 partial; proposal in decisions #10; not B5's path).
  - "@k" is not renamed in other lanes' records or hashed evidence (decisions #3).

Contract change requests raised: none.

Known gaps; where this lane fails first:
  - The fresh clone ran in the same container, not on a clean machine; the README's network needs (PyPI, npm, Playwright download) were not exercised cold.
  - Edge/Safari and decision paths other than accept are not walked (B3); the brief and trace say so.
  - The brief is at the four-page ceiling (2,678 words including diagrams and tables).
  - For @kaopilot: check the care-core copyright line in THIRD_PARTY_NOTICES.md (decisions #11).

Next step (the first thing the next session should do): @kaopilot merges b5-docs into main (docs only; a fast-forward from eff8608) and runs `make test` (expect 136 passed, 1 skipped). If any lane changes code before delivery, B5 re-runs the fresh-clone check and updates R1–R5 in the trace, the README expected line and this note. Optional, @kaopilot's call: (a) a B2 test for the author case (decisions #10), then flip trace P5 to holds; (b) an integrator rename of "@k" in lane records with B4's report refresh and a re-sign (decisions #3).

# Held-out fixtures (placeholder)

ENC-A2 is delivered to @k **outside the repo** (noteguard_heldout_ENC-A2.zip) so no B1 chat
can read it. Its `MANIFEST.sha256` has sha256 `c418f65aebaedc1b623c54e903936dd15e978d1a3db5ad8f8bbe2f981fa5f6ee`; check it before installing.
The integrator installs it here at I1 (instructions in the package README); until then
`tests/engine/test_heldout_golden.py` is skipped with a reason, and everything in this
directory except this README is git-ignored.
